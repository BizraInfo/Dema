# Fingerprint-scoped ignores (commit:path:rule:line). Each entry suppresses ONE
# historical false positive at ONE exact location; detection everywhere else is
# unchanged. Sealed classification receipts:
# /data/bizra/research/pr410-gitleaks-diagnostic-1a/receipt.yaml
#
# 1) Board-branch audit document: the rule matched ordinary English prose in a
#    truth-labeling section of the PDMLC baseline audit (doc-002). No credential
#    of any kind is present at that location.
# 2) The v1 comment of THIS file repeated that prose verbatim and re-triggered
#    the same rule at line 7 of the commit noted below. Lesson applied here:
#    describe findings by location, never by content.
bd21b536d00ada72867021275df0810d540aa156:backlog/docs/audits/bizra-pdmlc-baseline-1a/doc-002 - BIZRA-Product-and-Engineering-Lifecycle-Audit-v1.md:generic-api-key:484
91c0fe7af13f452303a6d49ab7b0b0fb08aabc94:.gitleaksignore:generic-api-key:7
# 3) Secret-detector test fixture: a deliberately credential-shaped, deliberately
#    fake literal in the true-positive half of the gate-35 pattern test. That
#    half exists to prove the pattern narrowing was not a weakening, so the
#    fixture cannot be removed without removing the proof. No credential of any
#    kind is present at that location.
#    Fingerprint-scoped on purpose: a path-scoped [[allowlists]] entry in
#    .gitleaks.toml was measured to blind the entire path (a real high-entropy
#    credential in the same file went undetected), so the narrow form is the
#    only safe one here.
5da3cd562a75149962cdf5521fa80d9afe1d9f03:tests/repo-claude-secret-pattern.test.js:generic-api-key:58
