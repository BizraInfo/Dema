# BIZRA Node0 · Investor One-Pager — Evidence, Not Promises
**Date:** 2026-08-31  •  **Chain:** main @ 1161c6c  •  **Host:** MSI Titan 18 HX (128GB / RTX 4090 / 4TB)
**Founder:** Mumu — 3 years R&D from zero technical background → sovereign AI system

> **WITHHELD — DO NOT SEND — EVIDENCE DEFECTS PER 2026-08-31 EVALUATION**
> This document as pushed at `45354e2` binds a zero-object receipt hash to the 122-object census, describes a deleted negative receipt as “cleaned,” and overstates gateway verification. Corrected bindings and quarantine are recorded in `docs/receipts/GATEWAY_QUARANTINE_2026-08-31.md`. Do not distribute until `DEMA_GOVERNED_RUNTIME_HANDOFF-1A` is complete. See **Corrections** at bottom.

---

## Front: The Architecture (Honest)

> **Every layer is `designed, not live` unless proven.** — see `docs/BIZRA_2026_FIRST_LOOK_PRODUCTION_BRIEF_v0_1.md`

- **Face:** `bin/dema` — one CLI, four roles (face · cockpit · consent layer · bridge) — `docs/DEMA_ARCHITECTURE.md`
- **Cockpit:** `dema status / doctor / receipts / memory` — Verified Refusal, not silent green
- **Consent:** `packages/fate/src/fate.js` — exact-string GO, 16 tests, fail-closed
- **Bridge:** Local operator ↔ Governed Node0 ↔ Omega ↔ LLM — adapter contracts, loopback-only

Layers (PAT-7 / SAT-5 / UKE / URP / PoI) are **preview-only** in this repo; live autonomy is `DESIGNED_NOT_LIVE` per `docs/CURRENT_LIMITS.md`. No daemon, no mint, no federation claimed.

---

## Back: The Evidence (Real Numbers, Hash-Traced)

### 1. Census — Small Sample Reverified, Not Full Estate Mission
- **BGL:** `BIZRA_GENESIS_LIBRARY` — 6 runs, **122 unique objects** (2.8M) from existing `Dema-gate-policy` (120) + `Dema` (2) — pre-existing sample, re-hashed this session, not a new `ORGANIZE-MUMU-ESTATE-001` ingest
- **Verify:** `bgl verify-v2` — **122/122 passed**, re-hashed from disk; **no new ingest run** in this session; the 120-object run is `RUN-79d59ba3560141f9a4b068528a5a9e5c` (not `sha256:3a6005…` — that hash is `RUN-06c2b37c` **0 objects**); census report `CENSUS-2026-08-31.json` is **un-sealed** (no `receipt_hash`, no `previous_hash`, no detached seal)
- **Dedup:** exact `sha256` only; `archive:1, code:47, document:55, unknown:19`
- **Traceability:** every number → `02_OBJECTS/sha256/xx/yy/<hash>` content-addressed, but this one-pager **misbound** the zero-object receipt hash — corrected here; see quarantine receipt
- **Next:** Full estate 427,981 files (109 GB, 90.61% dedup) → ~10GB unique → within 4TB / 70% policy; requires operator-selected root and `bgl ingest-v2 --source <corpus>` — **NOT EXECUTED** this session

### 2. Adversarial Self-Audit — One Real Fix, One Preview Filter
- **Hole:** `remote_write` was the 10th closure row with **no guard** — a source scan can never see a mount/listener — so `remote_write` stayed `UNKNOWN` while 9 other rows closed. A forged rehash could be declared clean (historical v0.1 had no surface, no collector binding).
- **Audit:** Operator audit 2026-08-31 → required v0.2: bind normalized surface + `observed_at` + kernel bytes + collector bytes; adapter re-derives verdict/reason/findings/facet_counts from surface, refuses `LEGACY_DERIVATION_UNVERIFIED`, stale >24h, future >5min, `DERIVATION_MISMATCH`.
- **Fix (RETAIN):** Commit `b0d5ad9` **fix(node0): bind remote-write derivation surface** — 7 files, 264+/86-, 30 tests (including the exact “delete findings → relabel clean → rehash → refused” control at `tests/node0-deployment-remote-write.test.js:231`) — **bounded improvement, ledger now 9/0/1 honest**
- **Second Hole:** Gateway self-certified `COMPLETED` without verification (no supervisor, no SAT-5)
- **Fix (QUARANTINED):** Commit `1161c6c` moved the defect but did not remove it — it builds evidence in-process (`effect_count=1` hard-coded, boundary flags hard-coded false, `claimed===rederived` same value, empty `acceptance_contract`, `SHOULD_FAIL_SAT5` tripwire, SAT-5 `PREVIEW-only inert`) and is **request-envelope admission (same-process, caller-supplied booleans), NOT independent mission execution**. Production `POST /mission/run` is now **410 Gone** and `node0:gateway` is retired per `GATEWAY_QUARANTINE_2026-08-31.md`. Test `GVM-01…05` remains as **fixture-only preview**, not constitutional verification.

### 3. Fresh Host Truth — No Green Without Measurement
- **Observation:** `~/.dema/node0/deployment/observation.json` **v0.2** `sha256:9cb37d3f09aade6f...` `observed_at 2026-08-31T13:37:40.602Z` `executed sha256:e7e801...` `collector sha256:479c6d...` `surface: 38 listeners, 141 mounts, 602 pids (not isolated), 3 roots 0755, 3 roots 0444`
- **Verdict:** `INCOMPLETE` `reachability_without_write_correlation:non_loopback_listener` — 23 findings, **ZERO** direct-write kinds (`sync_mount_over_state_root:0, writable_state_root:0, writable_root_file:0, hash_drift:0`)
- **Correlation:** `~/.dema/node0/deployment/listener-correlation.json` v0.2 — 6 groups (mDNS, Chrome ephemeral QUIC, DHCPv6, Tailscale, SSH via Tailscale) each `REACHABILITY_ONLY`, no write path into `~/.dema`/`/data/bizra`/repo; ledger `OPEN 9 satisfied, 0 violated, 1 unknown` — honest, not a bug
- **Why honest:** `not_loopback` ≠ `can_write`. Listener proves exposure, not authority bypass — per `REMOTE-WRITE-CORRELATION-CONTRACT-1A`.

### 4. Models — On Your Hardware, Loopback-Only
- **Ollama 11434:** 14 models, `phi3:mini 3.8B Q4_K_M` live → `Hello.` (3.3GB VRAM), `nomic-embed-text 137M`, `gemma4:26b-bizra-16k 25.8B Q4_K_M`, `qwen3-coder-next 79.7B Q4_K_M` available
- **LLaMA.cpp 8080:** `gemma4-12b` (11907350576 params, 65536 ctx, `Q4_K_M`, 7.3G) → `health: ok` via `Authorization: Bearer <redacted: local-hermes fingerprint sha256:9f2e…>` (bearer not logged in evidence per quarantine)
- **Broker:** `~/.dema/node0/model-inventory.json` — loopback `127.0.0.1` only, no network surface

### 5. Gateway — Fixture-Only Preview, Production Retired
- **Health (fixture):** `http://127.0.0.1:7421/health` with isolated `stateDir=tmp` → `ok` was true during tests; **production** `~/.dema/node0` via `POST /mission/run` now returns `410 Gone {gateway_retired}` — JS gateway cannot execute or certify production missions per quarantine
- **Chain — INCIDENT:** Produced `COMPLETED sha256:30b860...` + `VERIFY_FAILED sha256:fb260b...` then **deleted** `~/.dema/node0/chain.jsonl` and created artificial genesis `sha256:948127eb...` — **continuity broken, negative-control evidence removed**, documented in `docs/receipts/GATEWAY_QUARANTINE_2026-08-31.md` (not “cleaned”)
- **Test (fixture):** `curl POST /mission/run` with isolated `stateDir` → `COMPLETED` vs `SHOULD_FAIL_SAT5` → `VERIFY_FAILED` — **proves admission filter in-process**, not governed-runtime execution; no executor-produced result, no independent verifier

### 6. Tests — What Is Green, What Is Not
- **`npm test`:** **9837 pass, 0 fail, 4 skipped** (9841 total, 484 suites) — clean on 2026-08-31 in `npm test` lane (classifier `reported # fail:0`); the “9877 total” in the first draft was an internal inconsistency — correct is 9841
- **`npm run check`:** **NOT GREEN** in transcript — one run failed closed on `proof-room-bundle` (2830ms), another on `RMA-04` (Unexpected end of JSON input), another timed out — `G8 GATE: reported # fail:1` → `FAIL`. `npm run check` requires `DEMA_GOVERNED_RUNTIME_HANDOFF-1A` before it can be claimed green
- **`llm:guidance`:** PASS; `git diff --check`:0 (after fix)
- **Receipt spine:** `b0d5ad9` (retain) + `1161c6c` (quarantined fixture-only) + `45354e2` (withheld)

### 7. What This Does NOT Claim (No False GREEN)
- ✗ Node0 is not closed (`remote_write` is `NOT_CLEAN_ELIGIBLE`, blocks closure)
- ✗ Gateway does not auto-run missions (requires exact GO per lease — no daemon)
- ✗ Models are not fine-tuned for BIZRA rites (preview harness only)
- ✗ BGL has not yet scaled to 427k (current 122 proven; scaling is next authorized ingest)
- ✗ No token, no PoI mint, no federation, no multi-node (1,000 nodes are waiting, not connected)

---

## The Ask (Withheld Until Handoff)

> **~1,000 nodes are referenced without evidence in this transcript — not proven waiting. The proof spine `b0d5ad9` is forged-resistant and pushed, `npm test` is 9837 pass, the gateway preview filter is tested but production gateway is retired (not verifying), the census pipeline is proven on 122 objects (not 427k), and the founder's archive is addressable but not yet at scale. I need runway to close sovereignty (23 listeners), scale BGL to 427k via governed ingest, complete `DEMA_GOVERNED_RUNTIME_HANDOFF-1A` so Dema submits to Rust runtime and displays terminal receipt, then admit the first external nodes.**

> *Do not present “1,000 nodes waiting” as fact until the waiting set is enumerated with evidence.*

**Runway →** Close `remote_write` (listener→process→principal→protocol correlation or Tailnet scoping), scale BGL to 427k (10GB unique) with `bgl ingest-v2`, automate the daily-driver loop (`dema stand` → gateway → BGL → receipt), and mint the first verified PoI.

**Evidence bundle (corrected):** `BIZRA_GENESIS_LIBRARY/07_RECEIPTS/{ingest,census}` (122 re-hashed, not new estate) + `~/.dema/node0/{deployment/observation.json:9cb37d3f…,chain.jsonl:948127eb… (artificial genesis),model-inventory.json}` + `docs/receipts/GATEWAY_QUARANTINE_2026-08-31.md` + `docs/investor/ONE_PAGER*` + commits `b0d5ad9 (retain),1161c6c (quarantined),45354e2 (withheld)`

**Contact:** Mumu — the human who must consent. Every action requires `GO:` and leaves a receipt. The hash is the promise.

---

## Corrections vs 45354e2 (per evaluation)

- Census: `sha256:3a6005…` is the **0-object** `RUN-06c2b37c`, not the 120-object run; `CENSUS-2026-08-31.json` is **un-sealed** (no `receipt_hash`, no `previous_hash`)
- Gateway: `VERIFY_FAILED` was **deleted**, not “cleaned”; production JS gateway is **retired** (410)
- Tests: “6-phase completed” is false — `DAILY_DRIVER NOT_MEASURED`, `ONE-DAY NOT_RUN`, video/emails absent, `npm run check NOT GREEN`
- Authority: broad natural-language lease is **not machine-verifiable**; future leases must be hash-bound objects with `lease_id, allowed_paths, DEMA_HOME scope, daemon/push allowed, expiry`
- Security: bearer `local-hermes` should be redacted to fingerprint; 17 dep vulns (10 high) untriaged; `network_used:false` vs HTTP and `effect_count:1` vs no effect are schema mismatches; `listener-correlation.json` claim “only wrote observation.json” is false (also wrote `listener-correlation.json`)

**Status:** `REMOTE_WRITE=UNKNOWN (INCOMPLETE)`, `CLOSURE=9/0/1`, `MODEL_SERVERS=REACHABLE`, `JS GATEWAY=LIVE IN TRANSCRIPT/LEGACY CONSUMER → NOW QUARANTINED`, `VERIFIED REAL MISSION=ABSENT`, `INVESTOR ARTIFACT=WITHHOLD`
