# Dema · Current Limits v0.1

The honest map of what Dema does today, what is designed but not live,
and what is planned. Read this before reading the README, before quoting
us in a slide, before recommending Dema to a friend.

The rule: **no surface in Dema may advertise a capability that the local
code does not actually implement**. If a feature is below "MEASURED"
truth level, this page names it.

> **Companion docs**:
>
> - [`BIZRA_NODE0_DEMA_GOAL_SCRIPT_v0_1.md`](BIZRA_NODE0_DEMA_GOAL_SCRIPT_v0_1.md) — the north-star goal this page tracks against. The goal names every subsystem; this page reports how much of each is `MEASURED` today.
> - [`ROADMAP.md`](ROADMAP.md) — what slices are moving rows on this page from `PLANNED` / `DESIGNED_NOT_LIVE` toward `MEASURED`.

> **Canonical public-claim labels**:
>
> - `VERIFIED` — repeatable mechanism with a reachable evidence artifact.
> - `MEASURED` — recorded conditions, reproducible command, and artifact.
> - `DERIVED` — explicit derivation from a `VERIFIED` or `MEASURED` premise.
> - `SCENARIO` — hypothetical output with assumptions named.
> - `DESIGNED_NOT_LIVE` — design exists; runtime is not live.
> - `UNKNOWN` — evidence is absent or incomplete.
> - `FORBIDDEN` — the claim may not appear on a public surface.
>
> **Operational qualifiers used by this limits map**:
>
> - `PLANNED` — design intent only; not in code yet.
> - `LOCAL_ONLY` — works locally but is not share-safe in its current form.
>
> `PLANNED` and `LOCAL_ONLY` are not extra public-claim labels. Before a row is
> reused publicly, its sentence must receive exactly one canonical label from
> the Claim Register.

---

## What is MEASURED today

| Surface                                                                                                | Evidence                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| ------------------------------------------------------------------------------------------------------ | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Local full-suite verification entrypoint                                                            | `npm test` reports the exact count and accepted/rejected outcome for the current environment; do not quote a pinned count. A local run proves only its recorded Node/environment and tree state. The Node 20.x/22.x matrix in `.github/workflows/check.yml` becomes remote proof only when its rails are green on the exact branch SHA. |
| Capability truth registry for the pre-action spine surfaces (count = the registry's computed `capability_count`)                                         | `packages/core/src/dema-capability-truth-registry.js` + `tests/dema-capability-truth-registry.test.js` + `scripts/review/dema-capability-truth-registry-check.mjs` + `docs/02-architecture/DEMA_CAPABILITY_TRUTH_REGISTRY_v0_1.md` + `docs/receipts/DEMA_CAPABILITY_TRUTH_REGISTRY_1A.md`. Deterministic truth map only; no daemon, network, token, wallet, live URP federation, live RSI, or live PoI outside registered sandbox execute. |
| FDE dual diagnostic for inward/outward failure classification (DEMA-FDE-DUAL-DIAGNOSTIC-1A)           | `packages/core/src/dema-fde-dual-diagnostic.js` + `tests/dema-fde-dual-diagnostic.test.js` + `scripts/review/dema-fde-dual-diagnostic-check.mjs` + `docs/02-architecture/DEMA_FDE_DUAL_DIAGNOSTIC_v0_1.md` + `docs/receipts/DEMA_FDE_DUAL_DIAGNOSTIC_1A.md`. Classifies failed commands into code/proof vs environment hypotheses only; `eligible_for_autopatch: false`; no patch, commit, push, merge, daemon, network, token, or wallet. Hardened (DEMA-FDE-SEMANTIC-REDERIVATION-1B): `verify` re-derives the whole diagnosis from its carried input and rejects a forged-and-rehashed classification or an input-less report — the proof is bound to the input, not merely to its own hash; `github_actions_billing_lock` requires genuine GitHub context so generic billing prose cannot open the local proof lane. Hardened again (DEMA-FDE-BOUNDARY-PRECEDENCE-1C): v0.2 boundary violations dominate simultaneous billing-lock evidence, billing lock is outward-only, positive boundary evidence cannot be manufactured by all-false JSON, and a hard stop cannot be downgraded to an environment-repair path; bare standalone machine sentinels remain hard stops. Historical v0.1 reports verify only by semantic rederivation under a frozen v0.1 algorithm (schema relabeling is rejected) and stay `authority_eligible:false`. Does NOT verify input PROVENANCE — a self-consistent attacker-supplied input is a separate consumer signing/consent concern. |
| Node0 state router preview — turns File Steward + multi-device manifest evidence into bounded state-route previews (AASR-NODE0-STATE-ROUTER-PREVIEW-1A) | `packages/core/src/aasr-node0-state-router-preview.js` + `tests/aasr-node0-state-router-preview.test.js` + `scripts/review/aasr-node0-state-router-preview-check.mjs` + `docs/02-architecture/AASR_NODE0_STATE_ROUTER_PREVIEW_v0_1.md` + `docs/receipts/AASR_NODE0_STATE_ROUTER_PREVIEW_1A.md`. [MEASURED] Routes preview evidence into deterministic consent, compliance, and state-route previews. Does not prove route execution, model reasoning, reward, autonomy, or live federation. No execution, daemon, network, token, or wallet. |
| Node0 route refinery preview — critiques AASR routes for proof, consent, risk, and overclaim gaps (APR-NODE0-ROUTE-REFINERY-PREVIEW-1A) | `packages/core/src/apr-node0-route-refinery-preview.js` + `tests/apr-node0-route-refinery-preview.test.js` + `scripts/review/apr-node0-route-refinery-preview-check.mjs` + `docs/02-architecture/APR_NODE0_ROUTE_REFINERY_PREVIEW_v0_1.md` + `docs/receipts/APR_NODE0_ROUTE_REFINERY_PREVIEW_1A.md`. [MEASURED] Critiques AASR route previews for proof, consent, risk, and overclaim gaps. Does not prove route execution, live APR runtime, model reasoning, reward, or settlement. No execution, daemon, network, token, or wallet. |
| Coverage truth and test-artifact isolation gates (COVERAGE-TRUTH-GATE-1A; TEST-ARTIFACT-ISOLATION-1A) | `package.json` + `scripts/ci/classify-known-harness-failures.mjs` + `tests/g8-classifier.test.js` + `tests/test-artifact-isolation.test.js` + `scripts/check.mjs` + `docs/receipts/R2_COVERAGE_ENTRYPOINT_CLASSIFIER_PATCH_v0.1.md` + `docs/receipts/R3_CHECK_ENTRYPOINT_CLASSIFIER_PATCH_v0.1.md` + `docs/receipts/TEST_ARTIFACT_ISOLATION_1A.md`. [MEASURED] The repo classifies known harness failures without hiding unknown failures, and the three verification entrypoint strings no longer invoke the destructive URP restore preflight. This static guard does not prove all transitive helpers hermetic; current drift requires a recorded before/after worktree comparison, and remote proof requires green rails on the exact SHA. No daemon, network, token, or wallet. |
| Node-space file steward preview — metadata-only file organization with receipt-ready action atoms, no content read or mutation (DEMA-NODE-SPACE-FILE-STEWARD-1A) | `packages/core/src/dema-node-space-bonding-file-steward.js` + `tests/dema-node-space-bonding-file-steward.test.js` + `scripts/review/dema-node-space-bonding-file-steward-check.mjs` + `docs/02-architecture/DEMA_NODE_SPACE_BONDING_FILE_STEWARD_v0_1.md` + `docs/receipts/DEMA_NODE_SPACE_BONDING_FILE_STEWARD_1A.md`. [MEASURED] Previews metadata-only file organization actions with receipt-ready atoms. Does not prove file mutation, content understanding, OCR, embeddings, or upload. No execution, daemon, network, token, or wallet. |
| CI evidence attestation review-gate boundary — the three-rail attestation (`ci_matrix`, `codeql`, `gitleaks`) has zero authority over `bizra_review_gate` (CI-ATTEST-NO-SYNTHETIC-REVIEW-1A) | `packages/core/src/node0-ci-evidence-attestation.js` + `tests/node0-ci-evidence-attestation.test.js` (CEA-15…CEA-19: all 27 rail combinations preserve the prior review state; FAIL/UNKNOWN review gate + three green rails cannot produce `READY_LOCAL`). [MEASURED] Merging a three-rail attestation preserves the base input's review-gate state byte-for-byte — three green rails never synthesize review-gate evidence, and missing evidence never increases readiness. Does not prove Review Gate execution; that evidence comes only from the review gate itself. |
| CI vendor availability — FDE-backed GitHub Actions billing-lock lane so local proof rails do not treat vendor lock as code regression (NODE0-CI-VENDOR-AVAILABILITY-1A) | `packages/core/src/node0-ci-vendor-availability.js` + `tests/node0-ci-vendor-availability.test.js` + `scripts/review/node0-ci-vendor-availability-check.mjs` + `docs/receipts/DEMA_FDE_CI_BILLING_LOCK_MARKER_1A.md` + `docs/receipts/NODE0_CI_VENDOR_AVAILABILITY_1A.md`. [MEASURED] Classifies vendor billing lock and keeps the LOCAL proof lane honest while remote CI is advisory. Does not unlock GitHub billing, replace remote attestation, or authorize merge while CI is vendor-blocked. No execution, daemon, network, token, or wallet. |
| Governed reversible action preview — eligibility envelope over APR-refined routes with consent, backup, undo, and receipts (NODE0-GOVERNED-REVERSIBLE-ACTION-PREVIEW-1A) | `packages/core/src/node0-governed-reversible-action-preview.js` + `tests/node0-governed-reversible-action-preview.test.js` + `scripts/review/node0-governed-reversible-action-preview-check.mjs` + `docs/02-architecture/NODE0_GOVERNED_REVERSIBLE_ACTION_PREVIEW_v0_1.md` + `docs/receipts/NODE0_GOVERNED_REVERSIBLE_ACTION_PREVIEW_1A.md`. [MEASURED] Prepares one APR-refined reversible action candidate with consent, backup, undo, and receipt-preview requirements. Does not prove actual rename, mutation, live governed runtime, or post-action receipt. No execution, daemon, network, token, or wallet. |
| Multi-device URP manifest preview — composes laptop/mobile metadata into one Node0 resource-body preview (NODE0-MULTI-DEVICE-URP-MANIFEST-1A) | `packages/core/src/node0-multi-device-urp-resource-manifest-preview.js` + `tests/node0-multi-device-urp-resource-manifest-preview.test.js` + `scripts/review/node0-multi-device-urp-resource-manifest-preview-check.mjs` + `docs/02-architecture/NODE0_MULTI_DEVICE_URP_RESOURCE_MANIFEST_PREVIEW_v0_1.md` + `docs/receipts/NODE0_MULTI_DEVICE_URP_RESOURCE_MANIFEST_PREVIEW_1A.md`. [MEASURED] Composes laptop and mobile metadata into one Node0 resource-body preview. Does not prove live device sync, URP write, token mint, wallet access, or federation. No execution, daemon, or network. |
| Node0 spine runner CLI — one consent-gated path through execute -> receipt attestation -> proof chain -> signed chain head, sandbox only (NODE0-SPINE-RUNNER-CLI-1A) | `packages/core/src/node0-spine-runner.js` + `apps/cli/src/commands/node0-spine-run.js` + `apps/cli/src/commands/node0.js` + `tests/node0-spine-runner.test.js` + `tests/node0-spine-runner-cli.test.js` + `scripts/review/node0-spine-runner-check.mjs` + `docs/02-architecture/NODE0_SPINE_RUNNER_v0_1.md` + `docs/receipts/NODE0_SPINE_RUNNER_CLI_1A.md`. [MEASURED] Exposes `dema node0 spine run` as one measured operator command that runs the #306-#309 spine in a sandbox with exact-string consent, returning a JSON envelope with execute hash, chain head, and attestation status without daemon or network. Does not prove BIZRA-DATA-LAKE Node0 activation, real-time arbitrary tasks, federation, or live operator mutation outside the sandbox root. |
| Undo-proven preview — inverse-correction envelope composing execute gate + backup-anchored undo proof (NODE0-UNDO-PROVEN-1A) | `packages/core/src/node0-undo-proven-preview.js` + `tests/node0-undo-proven-preview.test.js` + `scripts/review/node0-undo-proven-preview-check.mjs` + `docs/receipts/NODE0_UNDO_PROVEN_1A.md`. [MEASURED] Seals one undo-proven preview when the reversible execute gate restores bytes from backup. Does not prove live governed runtime, federation, economic rights, or arbitrary action-class undo. No execution, daemon, network, token, or wallet. |
| Task decomposition engine — authoritative intent packet compiler (NODE0-TASK-DECOMPOSITION-ENGINE-1A) | `packages/core/src/node0-task-decomposition-engine.js` + `tests/node0-task-decomposition-engine.test.js` (14 tests). Pure deterministic kernel: compiles untrusted human/LLM input into ontology-bound intent atoms. `SERVER_VALIDATED_PREVIEW_ONLY` requires positive registry resolution of action + ontology + capability + risk; otherwise `BLOCKED_UNCLASSIFIED` with `route_eligible: false`. Consent always required; canonical 16-key all-false preview boundary; `verifyIntentPacket` re-derives from the input span. No execution, daemon, network, model invocation, LLM front-end, token, or wallet; CLI surface and review gate are later slices. |
| Reversible execute gate — sandbox-only governed rename (NODE0-REVERSIBLE-EXECUTE-GATE-1A) | `packages/core/src/node0-reversible-execute-gate.js` + `tests/node0-reversible-execute-gate.test.js` + `scripts/review/node0-reversible-execute-gate-check.mjs` + `docs/02-architecture/NODE0_REVERSIBLE_EXECUTE_GATE_v0_1.md` + `docs/receipts/NODE0_REVERSIBLE_EXECUTE_GATE_1A.md` (15 tests). [MEASURED] First sandbox governed rename with exact execute GO phrase, backup-before-action, split `content_hash`/`state_hash`, sealed append-only receipt, backup-anchored proven undo, inode containment (red-team wf_0d53ffcc-758). DI-pure kernel (`fs` injected). Sandbox dir only — NOT operator data or `$DEMA_HOME`. |
| [MEASURED] Peak evidence binding (PEAK-EVIDENCE-BINDING-1A + PEAK-EVIDENCE-GATHERER-1A): the peak self-loop's nine default signal events were hard-coded fixtures — including a `gate_passed` labelled "npm test + npm run check CLEAN" — that scored SNR and forced a default `CONTINUE_MICRO_SLICE`, letting remembered narrative authorize continuation with no current evidence. Signal events now score only with `truth_label` VERIFIED\|MEASURED + non-empty `source_ref` + 64-hex `source_sha256`; duplicate ids fail closed; fixtures remain visible at zero weight and surface as `evidence_debt`. The gatherer computes `source_sha256` from the artefact's ACTUAL content (purity by injection — caller supplies `readSource`), excludes unreadable sources, and `verifyEvidenceSignals` re-derives every hash rather than trusting the one the event carries. | `packages/core/src/peak-self-loop-preview.js` + `packages/core/src/peak-evidence-gatherer.js` + `tests/peak-self-loop-preview.test.js` (PEB-01…08, PSL-05/05b) + `tests/peak-evidence-gatherer.test.js` (PEG-01…07). | Measures evidence ELIGIBILITY only. The pure kernel validates evidence SHAPE, never BINDING — purity forbids it from reading `source_ref`, so a hand-written envelope pointing at a nonexistent file still scores as verified (pinned by PEB-08 as a known ceiling). The binding guarantee holds **only on the gatherer path**; a caller that constructs events directly bypasses it. No freshness/expiry check (needs an injected clock). Does NOT prove the gate a receipt attests to actually passed, nor close a live autopoietic loop. No daemon, network, model invocation, token, wallet, or federation. |
| [MEASURED] Peak VERIFY admission gate (PEAK-VERIFY-ADMISSION-1A / ADR-049 Action #2): pure judge-free verifier admission — `self_verifiable` true only for hash_equality · restore_test · suite_exit_0 · schema_validate · content_address_rederive; permanently refuses llm_as_judge · model_self_assessment · vibes · self_metric_without_independent_recompute; wired into `peak-self-loop` `proactive_self` (critique · harness · compliance · consent · verification_admission). Preview-only — does NOT execute acts, close the re-insert loop, or start autonomy. | `packages/core/src/verification-admission.js` + `packages/core/src/peak-self-loop-preview.js` + `tests/verification-admission.test.js` + `tests/peak-self-loop-preview.test.js`. Measures admission CLASSIFICATION only — NOT that VERIFY ran, NOT sealed re-insert as INPUT₁, NOT L1 closed cycle. No daemon, network, token, wallet, or federation. |
| [LOCAL_ONLY] L1 first closed cycle — sandbox single act (L1-MICRO-LOOP-1A / ADR-049 Action #5): one reversible rename that checkpoints, acts, self-verifies by hash equality, seals a hash-chained receipt and stops clean, with no human between PROPOSE and SEAL, inside an operator-issued lease (scope·expiry·budget). Every phase persists before proceeding; resume rolls forward only through judge-free verification, else rolls back from checkpoint. **Activation receipt `DEMA-ACTIVATION-L1-20260731T172500` and its `L1_MEASURED_SANDBOX` label are RETRACTED** (`RETRACTION-20260731T173800-L1-defects`) — four defects were reproduced by execution, each returning `PASS`: occupied-`dst` overwrite, erasure-by-rename of the receipt chain, `resume` mutating without a lease, and symlink scope escape past the lease boundary. All four are now guarded and each regression transports its attack. | `packages/core/src/l1-micro-loop.js` + `tests/l1-micro-loop.test.js` (21 tests) + `docs/MASTER-SPEC-NODE0-2026-07-31.md` §5. Measures ONE sandbox-scoped rename under a lease — NOT L1 activation, NOT L2 chaining, NOT a daemon or resident process, NOT network or model invocation. E5 closed: out-of-band `unlink` of `chain.jsonl` after a prior seal now fails closed (`chain_absent_with_history`) via `.l1/last_seal_head` continuity marker; run/resume/seal refuse before mutation; mid-first-cycle crash without a seal remains genesis. Open: marker lives inside `.l1` (same actor can delete both chain + marker — not an external tamper-proof anchor); suffix truncation of chain lines still verifies clean. Lesson: admission is not containment — blast radius is a separate gate from verifiability. |
| [LOCAL_ONLY] External anchor for hash-chained receipt logs (CHAIN-ANCHOR-1A): a pure comparator that holds the expected `(entries, head)` of a chain **outside** the directory the audited act can reach, turning "looks fine" into `OK` · `EXTENDED` · `ERASED` · `TRUNCATED` · `FORKED` · `NO_ANCHOR` · `MALFORMED`. Closes the last L1 audit-trail gap: E5 (`chain_absent_with_history`) catches an out-of-band chain delete while `.l1/last_seal_head` survives, but deleting **both** leaves the kernel reporting a clean `genesis:true` — measured, and the reason an in-band witness cannot close it. Growth past the anchor without `head_history` returns `MALFORMED` rather than assuming innocence; `verifyAnchorLog` hash-links anchor records so an edited judge is detectable; `assertAnchorOutside` refuses an anchor placed inside the leased scope (the E2 defect one level up). Pure — no fs, no crypto, no clock, no randomness; `hash` and `at` injected. | `packages/core/src/chain-anchor.js` + `tests/chain-anchor.test.js` (11 tests, red-first verified: neutering the erasure branch drops it to 9/2) + `docs/receipts/CHAIN_ANCHOR_1A.md`. **WIRED into L1** (`runL1Cycle`/`resumeL1Cycle` accept `anchorDir`): the gate runs before any mutation and again before a resume completes, refusing `anchor_erased` · `anchor_truncated` · `anchor_forked` · `anchor_log_forged` · `anchor_inside_scope`; the sealed head is anchored after the chain and marker are on disk, so a crash mid-seal leaves the anchor behind the chain (read as EXTENDED), never ahead of it. Anchoring is per-call: a cycle run WITHOUT `anchorDir` still carries the original gap. Measures the comparison law ONLY — NOT that any anchor store is durable, NOT that an act was correct, NOT that a loop closed, NOT L1 activation. Suffix truncation of a chain whose anchor is stale remains undetected until the next anchor is taken. |
| [MEASURED] Sanitizer-gated ask join (DEMA-ASK-H3H4): `dema ask "<q>" --scope <dir>` reads scoped text files, hard-gates every file through the untrusted-corpus sanitizer at Layer −1, and admits **ALLOWED text only** into the retrieval index and the prompt — QUARANTINED and BLOCKED files are recorded in the perimeter report and never appear in prompt, answer, or `source_refs`. Emits `bizra.dema.ask_h3h4.v0.1` with truth label `DEMA_ASK_H3H4_MEASURED_REPO`, content-addressed `source_hashes` + `answer_digest`, and writes one truth-graph receipt under `DEMA_HOME/ask`. Consent: subcommand-gated plus the exact phrase `GO: dema ask H3/H4 sanitizer-gated` (code constant `DEMA_ASK_H3H4_GO_PHRASE`, not matrix metadata); `--invoke` additionally requires a separate per-model consent phrase via `llmAdapterConsentPhraseFor`. Answers are **extractive by default** — no model runs unless `--invoke` is passed. | `packages/core/src/dema-ask-h3h4.js` + `apps/cli/src/commands/ask.js` + `tests/dema-ask-h3h4.test.js` (8 tests) + consent-matrix row (`external_runtime`, `subcommand_gated`, ADR-042 localhost bridges `DEMA_OLLAMA_URL` / `DEMA_LM_STUDIO_URL` / `DEMA_LLAMACPP_URL`) verified by `scripts/review/consent-bridge-parity-check.mjs`. Boundary: 8 keys all false except `model_invocation_performed`, which is true only on an `--invoke` run. [LOCAL_ONLY] The live model path is **not measured** — tests exercise it through an injected `fetchImpl`, never a live daemon. Does NOT prove: semantic correctness of the answer, live model honesty beyond the injected/local invoke gates, or that novel obfuscated secrets are caught beyond the sanitizer's pattern library. No token, wallet, federation, or network egress outside the registered localhost bridges. |
| [MEASURED] FIRST-LIGHT reproduction harness (FIRST-LIGHT-1A): the artifact a witness runs — `node scripts/proof/first-light-mission.mjs <work-dir>` executes one bounded, mandatory-anchored, reversible Ω0-M mission over a byte-frozen 10-file fixture with a pinned anchor path and a fixed clock, then emits `bizra.dema.first_light_mission.v0.1` under truth label `FIRST_LIGHT_MEASURED_LOCAL`. Measured: `status SEALED`, `source_loss 0`, `content_hash_changes 0`, `undo_success_pct 100`, `restored_hash == before_manifest_hash`, `reapply_hash == after_manifest_hash`, anchor enforced and stored outside the leased scope, fresh-reader replay recomputing both seal and world, `authority_delta 0`. **The witness contract:** the manifest hashes are content-bound (`before 403db0b4…`, `after 89eb646f…`) and identical across different work directories, so they are the values a stranger on another machine compares; `seal_head` is path-bound — stable for a fixed directory, legitimately different across directories, because it binds the absolute scope and anchor paths. Publishing `seal_head` as a cross-machine constant would make honest reproductions look like failures. | `scripts/proof/first-light-mission.mjs` + `tests/first-light-mission.test.js` (8 tests: FL-01..FL-08; FL-05/06/07 pin the content-bound/path-bound split in both directions). Fixture is frozen bytes, NOT copies of repo files — deriving it from the tree would drift the hashes on every commit. Does NOT prove Node0 closure, L1 activation, federation, durability of any anchor store, semantic correctness of the organisation, or behaviour at scale: one folder, 10 files, `move`-only (delete and overwrite are forbidden, not recovered). No network, no model invocation, no runtime activation. |
| [MEASURED] [LOCAL_ONLY] **Node0 clean-state journey — the replayable witness** (NODE0-CLEAN-STATE-JOURNEY-1A): `docs/NODE0_DEMA_URP_FLAGSHIP_DOD.md` §14 named the gap as "Genesis needs replayable proof, not only green CI"; every other suite proves a code path in isolation. This harness drives the REAL `bin/dema` through one operator journey from nothing — welcome -> setup -> setup-check -> status -> URP 5SAT declaration (exact consent phrase) -> read the receipt that declaration wrote -> Covenant Gate screens a proposal -> the consent gate REFUSES without a signing key -> receipts listed — against a `DEMA_HOME` the harness creates and removes, refusing to run against an already-initialised home. **The witness contract:** a clean-state run yields two kinds of value, and publishing the wrong kind as a constant makes honest reproductions look like failures. Cross-machine invariants (the 5SAT active set/lock/blocked list, the covenant `proposal_hash`, screening verdicts and claim labels, the consent refusal, `config.local.json`'s hash, the step sequence and every exit code, and the all-false boundary) are hashed with canonical JSON v1 into ONE published `journey_invariant_hash` — the value a stranger compares. MEASURED, not assumed: two independent clean homes produce a byte-identical invariant hash while `launch_hash`, `decision_id`, `created_at`, `profile.json` and `.dema-root.json` hashes all differ; those are reported under `environment_bound` and deliberately excluded from the hash. Closes DoD §15 boxes 3–6. | `scripts/proof/node0-clean-state-journey.mjs` (registered canonical-JSON-v1 consumer) + `tests/node0-clean-state-journey.test.js` (CSJ-01…08; CSJ-02 and CSJ-03 are load-bearing as a PAIR — without CSJ-03 proving the environment values genuinely differ, CSJ-02 could be satisfied by freezing everything into a constant and would prove nothing) + `docs/NODE0_DEMA_URP_FLAGSHIP_DOD.md` §15. Does NOT prove Node0 closure (`NODE0_CLOSED` remains false), runtime activation, a daemon, a live mission, a SIGNED covenant receipt (the gate is proven to refuse without a key), operator identity or the TASK-029 ceremony, federation, Node1, token economy, PoI, mint, or external legal/Shariah review; the screening verdicts are PROTOTYPE-labelled by the covenant gate itself and are not claimed substantively correct. No network, no model invocation, no daemon; writes only inside the temporary journey home. `authority_delta: 0`. |
| Receipt signing attestation — Ed25519 bind for sandbox execute receipts (NODE0-RECEIPT-SIGNING-ED25519-1A, parity round NODE0-RECEIPT-SIGNING-PARITY-1A) | `packages/core/src/node0-receipt-signing-ed25519.js` + `tests/node0-receipt-signing-ed25519.test.js` + `scripts/review/node0-receipt-signing-ed25519-check.mjs` + `docs/02-architecture/NODE0_RECEIPT_SIGNING_ED25519_v0_1.md` + `docs/receipts/NODE0_RECEIPT_SIGNING_ED25519_1A.md` (20 tests). [MEASURED] Ed25519 attestation over `#306` execute receipt `content_hash`/`state_hash` binds; exact signing GO phrase; consent assertion inside the signed payload (payload schema v0.2 — a signature over a consent-free payload fails `consent_not_in_signed_subject`); displayed fingerprint must re-derive from the verifying key (`public_key_fingerprint_mismatch`; malformed PEM fails `public_key_invalid`); public-key-only verify; tamper rejection; consented key-store path outside repo. [MEASURED] Signing authority ≠ execution authority — no operator mutation or live runtime. Does not prove operator key custody. |
| [MEASURED] Authorship key rotation capability on the active-pointer generation store (AUTHORSHIP-KEY-ROTATE-1B) | `packages/receipts/src/authorship-key-store.js` (`rotateAuthorshipKey`, `KEY_ROTATE_CONSENT_PHRASE`, `loadGuardedActiveKey`) + `packages/receipts/src/authorship-signature.js` + `apps/cli/src/commands/authorship.js` (`dema authorship key rotate`) + `tests/authorship-key-rotate.test.js` (24 fixture tests) + `docs/ARCHITECTURE.md` / `docs/TESTING.md` rows. [MEASURED] Consent-gated + nonce-envelope rotation against the post-#419 generation store: quarantine under `keys/retired/<fp>/`, retire into `retired-registry.json` before pointer swap, activate via `activateGeneration`, journal PREPARED→ACTIVATING→RETIREMENT_COMMITTED→COMPLETE; pre-#419 flat homes refuse `no_key_to_rotate` (migrate first). History: code-only land at `99f1503`, then `ab5d145` reverted **with no rationale recorded in the commit body**; this 1B port re-lands onto the active-pointer shape (not a naive cherry-pick of `99f1503`). Does NOT prove a founder ceremony on `~/.dema/keys/` — no real key was generated or rotated in this slice. Distinct from TASK-029 (SIGNER-ROTATION-CEREMONY-1A / Node0 mission-signing key). |)
| [MEASURED] **CP5 closed — interrupted authorship-rotation resume** (AUTHORSHIP-ROTATION-RESUME-1A): the P0.2b crash matrix (2026-07-29) found that a rotation killed between `appendRetiredRegistry` and the active-pointer commit leaves the old fingerprint retired while the pointer still names it, so the home has no usable active key — the defect `docs/gtm/TASK029_PRE_CEREMONY_HALT.md` holds the TASK-029 ceremony behind. Measured here: the pre-fix posture is **fail-closed, not unsafe** (`loadActiveKeyPair` → `retired_generation`, `loadGuardedActiveKey` → `rotation_in_progress`; nothing ever signs with a retired key), so CP5 is a LIVENESS defect. `resumeAuthorshipRotation` rolls the interrupted rotation FORWARD under the exact phrase `RESUME AUTHORSHIP ROTATION` onto the generation whose bytes were archived and byte-verified BEFORE the retirement was written, re-verifying it through `verifyPointerDoc` — the same contract `loadActiveKeyPair` enforces — before the pointer may move. Read-only inspection REPORTS the state (`rotation_resume_state`) and repairs nothing: the rejected PR #414 auto-quarantine design stays extinct and the refuse-and-report gate's read-only roots stay read-only. A dead holder's preserved lease is taken over only by this consented call; a live holder still refuses. An exact re-run is idempotent and changes no durable byte. | `packages/receipts/src/authorship-key-store.js` (`resumeAuthorshipRotation`, `KEY_ROTATE_RESUME_CONSENT_PHRASE`, read-only `classifyRotationResume`) + `tests/authorship-rotation-resume.test.js` (CP5-01…07, real child `SIGKILL` at the pointer-commit boundary via `tests/fixtures/kill-before-pointer-commit-preload.mjs`) + `scripts/review/identity-recovery-refuse-report-check.mjs` (unchanged, still PASS: 26 reachable functions, 0 violations). Fixture homes only — **no real key was generated or rotated**; this does NOT perform, replace, or reduce the founder ceremony (TASK-029), and cannot reproduce an interrupted ceremony's original consent envelope (its nonce is not persisted in the journal), so the resume seals a receipt bound to the resume phrase instead of forging the original binding. No runtime, daemon, network, model, token, or federation. `authority_delta: 0`. |
| Append-only signed-receipt proof chain (NODE0-PROOF-CHAIN-LINK-1A) | `packages/core/src/node0-proof-chain-link.js` + `tests/node0-proof-chain-link.test.js` + `scripts/review/node0-proof-chain-link-check.mjs` + `docs/02-architecture/NODE0_PROOF_CHAIN_LINK_v0_1.md` + `docs/receipts/NODE0_PROOF_CHAIN_LINK_1A.md` (10 tests). [MEASURED] Pure kernel binds ordered #307 signed-receipt `content_hash` anchors into a content-addressed append-only hash chain: each link commits to the prior link hash from a genesis sentinel; `verifyNode0ProofChainLink` re-derives every link and fails closed on in-place receipt tampering, link-hash forgery, reorder/fork, and head/content-hash mismatch. [MEASURED] Consumes already-signed attestations — no signing, no key custody, no mint, no §1 identity surface, no daemon/network/federation; boundary all-false. |
| Signed proof-chain head — one signature attests the whole receipt history (NODE0-SIGNED-CHAIN-HEAD-1A) | `packages/core/src/node0-signed-chain-head.js` + `tests/node0-signed-chain-head.test.js` + `scripts/review/node0-signed-chain-head-check.mjs` + `docs/02-architecture/NODE0_SIGNED_CHAIN_HEAD_v0_1.md` + `docs/receipts/NODE0_SIGNED_CHAIN_HEAD_1A.md` (12 tests). [MEASURED] Composes #307 + #308: verifies a #308 proof chain, Ed25519-signs its `head_hash` (injected/ephemeral keypair) into a public-key-verifiable attestation binding head_hash + link_count + chain content address; a tampered/reordered chain yields a different head and fails the bind. [MEASURED] Public-key-only verify; no private-key material in the envelope; no persistent key custody, no real-identity key generation, no §1 identity runtime, no daemon/network/federation; boundary signing≠execution. |
| [MEASURED] Node0 evidence source registry (NODE0-EVIDENCE-SOURCE-REGISTRY-1A) | `packages/core/src/node0-evidence-source-registry.js` + `tests/node0-evidence-source-registry.test.js` + `scripts/review/node0-evidence-source-registry-check.mjs` + `docs/02-architecture/NODE0_EVIDENCE_SOURCE_REGISTRY_v0_1.md` + `docs/receipts/NODE0_EVIDENCE_SOURCE_REGISTRY_1A.md` — deterministic local registry for local path, GitHub repo, Google Drive, Claude export, public domain, proof receipt, design asset, and economy simulation sources. It classifies source type, privacy, truth label, dedup policy, promotion gate, and impact candidacy before indexing. It does not ingest source contents, download Drive data, write GitHub, scrape the web in runtime, delete, reorg, verify impact, mint tokens, access wallets, start daemons, or federate. |
| [MEASURED] Node0 local closure readiness map (NODE0-LOCAL-CLOSURE-READINESS-1A) | `packages/core/src/node0-local-closure-readiness.js` + `tests/node0-local-closure-readiness.test.js` + `scripts/review/node0-local-closure-readiness-check.mjs` + `docs/02-architecture/NODE0_LOCAL_CLOSURE_READINESS_v0_1.md` + `docs/receipts/NODE0_LOCAL_CLOSURE_READINESS_1A.md` — deterministic readiness composer for the Node0 evidence source registry plus Node0 space-index envelope. It reports PAT-local registry/index gates, exact hash-consent next action, review-only impact candidates, SAT metadata blocked until apply completes, and zero mint allowance before verified PoI. It does not ingest content, execute dedup, apply reorg, submit to SAT, verify impact, mint tokens, access wallets, start daemons, use network, or federate. |
| [MEASURED] Morning Standing Receipt: composes injected local evidence (git state, gate-log metadata, declared blockers) into a daily first-user standing card with FDE lens, exactly one next action, declared drain metric, stale-proof detection, and orbit warning. `dema stand` CLI with consent-gated receipt write under DEMA_HOME/stand. (DEMA-STAND-1A) | `packages/core/src/dema-stand.js` + `apps/cli/src/commands/stand.js` + `tests/dema-stand.test.js` + `tests/dema-stand-cli.test.js` + `scripts/review/dema-stand-check.mjs` + `docs/02-architecture/DEMA_STAND_v0_1.md` + `docs/receipts/DEMA_STAND_1A.md`. Measures deterministic composition + re-derivation only — NOT a daily-loop runtime, NOT drain inference, NOT autonomy. No execution, daemon, network, token, wallet, or federation. |
| [MEASURED] Steward-chain verifier: verifies the FIRST_USER standing-receipt chain (consecutive UTC days, per-receipt re-derivation through the STAND verifier, drain series) and emits honest NOT_STARTED / RECEIPTS_INVALID / CHAIN_BROKEN / IN_PROGRESS n/7 / COMPLETE verdicts — the Day-N report exists only on COMPLETE; days cannot be fabricated. `dema stand chain` CLI. (DEMA-STEWARD-CHAIN-1A) | `packages/core/src/dema-steward-chain.js` + `apps/cli/src/commands/stand.js` + `tests/dema-steward-chain.test.js` + `tests/dema-stand-cli.test.js` + `scripts/review/dema-steward-chain-check.mjs` + `docs/02-architecture/DEMA_STEWARD_CHAIN_v0_1.md` + `docs/receipts/DEMA_STEWARD_CHAIN_1A.md`. Measures deterministic chain verification only — NOT proof that Dema reduced founder drain, NOT a scheduler, NOT autonomy. No execution, daemon, network, token, wallet, or federation. |
| [MEASURED] Local-only PoI time-compression candidate receipt: declared baseline estimate (reference-class assumption, never a measured fact) vs declared actual proof-loop duration under required quality gates — a failed gate refuses the receipt; proof-time and observation-time are separate clocks; verify rejects forged-and-recomputed ratio/gate/review/mint fields. `dema poi compression record|show|verify` CLI with consent-gated receipt write under DEMA_HOME/poi/compression. (POI-TIME-COMPRESSION-1A) | `packages/core/src/poi-time-compression.js` + `apps/cli/src/commands/poi.js` + `tests/poi-time-compression.test.js` + `scripts/review/poi-time-compression-check.mjs` + `docs/02-architecture/POI_TIME_COMPRESSION_v0_1.md` + `docs/receipts/POI_TIME_COMPRESSION_1A.md`. Measures deterministic candidate-receipt composition + re-derivation only — NOT verified impact, NOT a live PoI economy, NOT proof that any compression event occurred outside the declared inputs. No execution, daemon, network, token, wallet, mint, or federation. |
| [MEASURED] Away Contract ladder (ADR-043): fail-closed schema validator (never-grantable actions reject even when requested), whole-body launder-detecting verifier, exact-consent atomic receipt writer under DEMA_HOME/away-contracts, deterministic draft compiler, and `dema away draft\|verify\|receipt` CLI. Contracts are drafted, validated, verified, and receipted — never started. (AWAY-CONTRACT-1A) | `packages/core/src/away-contract-{schema,verify,receipt,compiler}.js` + `apps/cli/src/commands/away.js` + 7 test files (`tests/away-contract-*.test.js`) + `scripts/review/away-contract-check.mjs` + `docs/02-architecture/AWAY_CONTRACT_SPEC_v0_1.md` + `docs/receipts/AWAY_CONTRACT_1A.md`. Measures deterministic drafting/validation/body-binding/receipting only — NOT live absence stewardship, NOT unattended runtime, NOT `dema away start` (does not exist), NOT operator consent beyond the recorded phrase. No execution, daemon, model, network, token, wallet, mint, or federation. |
| [MEASURED] Absence Steward readiness preview: `dema away preview` derives NOT_CONFIGURED / CONTRACT_VERIFIED / PREVIEW_READY / EXPIRED / REFUSED from an Away Contract trio — binding judged at the receipt's hash-protected created_at, expiry at injected now; forged/recomputed receipts refused; PREVIEW_READY grants nothing and steward_started stays false. (ABSENCE-STEWARD-READINESS-1A) | `packages/core/src/absence-steward-readiness.js` + `apps/cli/src/commands/away.js` + `tests/absence-steward-readiness.test.js` + `tests/away-contract-cli-preview.test.js` + `scripts/review/absence-steward-readiness-check.mjs` + `docs/02-architecture/ABSENCE_STEWARD_PREVIEW_v0_1.md` + `docs/receipts/ABSENCE_STEWARD_READINESS_1A.md`. Measures readiness REPORTING only — NOT steward runtime, NOT a daemon, NOT a scheduler, NOT unattended execution, NOT model invocation, NOT `dema away start` (does not exist). No network, token, wallet, mint, or federation. |
| [MEASURED] Absence Steward return review: `dema away review` derives NO_ABSENCE_RECORDED / REVIEW_BLOCKED / READY_BUT_NOT_STARTED / EXPIRED_BEFORE_START from the trio plus a declared window (`--left`/`--returned`, never the clock; readiness re-derived at both edges). Every claim receipt-backed or "NO_RECEIPT — cannot claim."; executed_summary always "Nothing executed."; work-completion verdicts unreachable. (ABSENCE-STEWARD-RETURN-REVIEW-1A) | `packages/core/src/absence-steward-return-review.js` + `apps/cli/src/commands/away.js` + `tests/absence-steward-return-review.test.js` + `tests/away-contract-cli-review.test.js` + `scripts/review/absence-steward-return-review-check.mjs` + `docs/02-architecture/ABSENCE_STEWARD_RETURN_REVIEW_v0_1.md` + `docs/receipts/ABSENCE_STEWARD_RETURN_REVIEW_1A.md`. Measures return-review REPORTING only — NOT runtime, NOT a queue, NOT a daemon, NOT a scheduler, NOT unattended execution, NOT model invocation, NOT `dema away start` (does not exist), NOT proof any work occurred. No network, token, wallet, mint, or federation. |
| [MEASURED] Absence Steward queue proposal spine: fail-closed item validator (EXECUTING/DONE/AUTO_APPROVED and consent-ish fields reject; membership is never consent), whole-body launder-detecting verifier, exact-consent atomic no-overwrite receipt writer (`approved:false`, `executed:false` on every path), and validate-only `dema away queue draft`. Recording a proposal never moves it. (ABSENCE-STEWARD-QUEUE-PROPOSAL-SPINE-1A) | `packages/core/src/absence-steward-queue-{schema,verify,receipt}.js` + `apps/cli/src/commands/away.js` + `tests/absence-steward-queue-{schema,verify,receipt}.test.js` + `tests/away-queue-cli-draft.test.js` + `scripts/review/absence-steward-queue-check.mjs` + `docs/02-architecture/ABSENCE_STEWARD_LOCAL_QUEUE_v0_1.md` + `docs/receipts/ABSENCE_STEWARD_QUEUE_PROPOSAL_SPINE_1A.md`. Measures the PROPOSAL spine only — NOT a live queue, NOT a runner, NOT approval flow, NOT dequeue, NOT a scheduler, NOT execution, NOT `dema away start` (does not exist). No model invocation, network, token, wallet, mint, or federation. |
| [MEASURED] FDE forwarder diagnostic: routes a completed FDE dual-diagnostic report to exactly one proposal destination under the eight Diagnostic Doxology rules (code→patch proposal, proof→repair proposal, world→environment proposal, billing-lock→operator action never code failure, boundary-violation/insufficient-evidence→stop). Closed six-destination vocabulary — no mint/execute/autopatch route exists; `mint_blocked` and `connected_claim_made:false` hold on every routing; whole-body re-derivation rejects forged-and-recomputed routings and vacuous boundaries. (DEMA-FDE-FORWARDER-DIAGNOSTIC-1A) | `packages/core/src/dema-fde-forwarder-diagnostic.js` + `tests/dema-fde-forwarder-diagnostic.test.js` + `scripts/review/dema-fde-forwarder-diagnostic-check.mjs` + `docs/02-architecture/DEMA_FDE_FORWARDER_DIAGNOSTIC_v0_1.md` + `docs/receipts/DEMA_FDE_FORWARDER_DIAGNOSTIC_1A.md`. Measures routing DERIVATION only — NOT a runtime, NOT a dispatcher, NOT execution, NOT patching, NOT transmission, NOT connectivity verification, NOT proof the upstream diagnosis is ground truth. No model invocation, network, token, wallet, mint, or federation. |
| [MEASURED] Preview-receipt signing adapter: any `mode: preview_only` report (the real peak-self-loop preview is the gate fixture) binds into a canonical content-addressed envelope that stays explicitly `signed:false` / `signature:null` until signed through the existing Ed25519 authorship rail (same primitives as NODE0-RECEIPT-SIGNING-ED25519-1A — no new signing system) under the exact GO phrase; canonical hash stable across rebuilds, whole-body re-derivation rejects tampered hashes, the Ed25519 signature anchor rejects forged-and-recomputed bodies, the exact-consent hash sits inside the signed subject (signatures over the bare envelope fail), the displayed fingerprint must re-derive from the embedded public key, and envelopes carry no private-key material. (PREVIEW-RECEIPT-SIGNING-1A) | `packages/core/src/preview-receipt-signing.js` + `tests/preview-receipt-signing.test.js` + `scripts/review/preview-receipt-signing-check.mjs` + `docs/02-architecture/PREVIEW_RECEIPT_SIGNING_v0_1.md` + `docs/receipts/PREVIEW_RECEIPT_SIGNING_1A.md`. Measures the signing ADAPTER only — NOT operator key custody, NOT a signer daemon, NOT public-safe publication, NOT remote CI seal, NOT economic activation, and signing a preview does not make its content true. No model invocation, network, token, wallet, mint, or federation. |
| Preview-only local model adapter contract: binds an injected discovery report into a content-addressed adapter envelope (model always null, boundary all-false) that refuses live-invocation, wallet, mint, and URP fields — no model invocation, no network. (LOCAL-MODEL-ADAPTER-PREVIEW-1A) | `packages/core/src/local-model-adapter-preview.js` + `tests/local-model-adapter-preview.test.js` + `scripts/review/local-model-adapter-preview-check.mjs` + `docs/02-architecture/LOCAL_MODEL_ADAPTER_PREVIEW_v0_1.md` + `docs/receipts/LOCAL_MODEL_ADAPTER_PREVIEW_1A.md`. [MEASURED] Exact-consent contract kernel: model pinned null, runtime derived from first reachable provider (else unknown), canonical all-false boundary via deep key-set equality, whole-body hash re-derivation rejects tampered AND laundered (forged+recomputed) bodies, exact-name forbidden fields (wallet, mint, private_key, urp_live) refused at plan and verify time (13 tests). Measures the adapter CONTRACT only — NOT model correctness, NOT live invocation, NOT inference quality; the adapter never selects or calls a model. No execution, daemon, network, token, wallet, or federation. |
| Deterministic blast-radius classifier: derives blast_radius (low|medium|high) and reversibility from declared action mutation flags — never from prose — so graduated consent can name what an action touches before it runs. No execution, no network, no mutation. (CAPABILITY-BLAST-RADIUS-1A) | `packages/core/src/capability-blast-radius.js` + `tests/capability-blast-radius.test.js` + `scripts/review/capability-blast-radius-check.mjs` + `docs/02-architecture/CAPABILITY_BLAST_RADIUS_v0_1.md` + `docs/receipts/CAPABILITY_BLAST_RADIUS_1A.md`. [MEASURED] Deterministic decision matrix over seven canonical mutation flags + closed recovery vocabulary: low/medium/high with named reasons, reversibility strictly from recovery, whole-body content-addressing plus full re-derivation (laundered downgrades rejected), non-canonical descriptors refused at plan time (12 tests). Classifies DECLARED flags only — grants no authority, observes no runtime, performs and blocks nothing. No execution, daemon, network, token, wallet, or federation. |
| Operator-invoked proof-health monitor: classifies injected proof-surface facts (stale proof, registry/docs drift, missing review gates, evidence-free verified claims, forbidden-claim markers) into severity findings with evidence refs — deterministic, no daemon, no autofix, no authority increase. (RECEIPT-MONITOR-PREVIEW-1A) | `packages/core/src/receipt-monitor-preview.js` + `tests/receipt-monitor-preview.test.js` + `scripts/review/receipt-monitor-preview-check.mjs` + `docs/02-architecture/RECEIPT_MONITOR_PREVIEW_v0_1.md` + `docs/receipts/RECEIPT_MONITOR_PREVIEW_1A.md`. [MEASURED] Exact-consent contract kernel over injected proof-surface facts: ten failure classes (stale proof, dirty tree, registry count drift, missing CURRENT_LIMITS/TESTING rows, missing review gate, testless measured row, evidence-free verified claim, forbidden-claim markers, CI-unavailable-as-outward) -> severity findings with evidence refs and closed allowed-action vocabulary; criticals fail closed (proceed_allowed=false); findings+summary fully re-derivable so forged clean verdicts and authority_delta increases are rejected (14 tests). Monitors DECLARED facts only — no fs scan, no daemon, no autofix, no receipt write, no authority grant. No execution, network, token, wallet, or federation. |
| Read-only monitor-facts derivation: compiles injected raw repo artifacts (git metadata, gate-log ages, registry rows, check.mjs source, docs texts, receipt metadata) into the receipt-monitor input facts, content-addressed and fully re-derivable — no fs in kernel, no network, no mutation. (MONITOR-GATHERER-1A) | `packages/core/src/monitor-gatherer.js` + `tests/monitor-gatherer.test.js` + `scripts/review/monitor-gatherer-check.mjs` + `docs/02-architecture/MONITOR_GATHERER_v0_1.md` + `docs/receipts/MONITOR_GATHERER_1A.md`. [MEASURED] Exact-consent derivation from injected raw artifacts to receipt-monitor input facts: stale-proof from gate-log ages vs threshold, tree-clean from dirty count, registry/docs membership by exact-substring presence of the hyphenated capability ID or a specific (directory-qualified) source path, `scripts/check.mjs`-as-gate recognized as inherently in-check, test-path presence map; derived facts content-addressed and fully re-derivable (laundered clean repo_state rejected); pipes eligibly into RECEIPT-MONITOR-PREVIEW-1A; `dema monitors run` wires the read-only CLI gatherer (19 kernel tests + 4 CLI tests, MONITOR-DRIFT-REPAIR-1A precision refinement). Derives DECLARED artifacts only — collection honesty is the gatherer layer; no daemon, autofix, receipt write, execution, network, token, wallet, or federation. |
| Preview-only reward-eligibility contract: classifies a DEMA lifecycle outcome as reward-eligible or reward-ineligible from evidence refs, monitor state, and claim flags — inert output with no score, no authority signal, no action-permission field; forbidden claims and monitor-hiding are dominant refusals; evidence refs mandatory. (REWARD-ELIGIBILITY-CONTRACT-PREVIEW-1A) | `packages/core/src/reward-eligibility-contract-preview.js` + `tests/reward-eligibility-contract-preview.test.js` + `scripts/review/reward-eligibility-contract-preview-check.mjs` + `docs/02-architecture/REWARD_ELIGIBILITY_CONTRACT_PREVIEW_v0_1.md` + `docs/receipts/REWARD_ELIGIBILITY_CONTRACT_PREVIEW_1A.md`. [MEASURED] Exact-consent contract kernel classifying a DEMA lifecycle outcome as reward-eligible/ineligible from a closed outcome-kind vocabulary + mandatory evidence refs + injected monitor state + eight forbidden-claim flags: monitor criticals block, monitor-weakened-to-hide-drift is the dominant refusal, mint/wallet/urp-live/federation/public-safe/authority/cost-as-value/simulated-as-real each refuse, all-clear assertion must match monitor state; verdict content-addressed and fully re-derivable so forged-eligible and inertness-breach verdicts are rejected (21 tests). The verdict is INERT — is_score:false, is_actuation_signal:false, confers_permission:false, authority_delta:0; NOT a reward, NOT a scorer, no LIVE consumer acts on it. URP-FACING: names its designed future constitutional consumers (BIZRA URP genesis preview, SAT-5 verifier set, future node admission) with live_runtime_consumer_enabled/actuator_readable_permission/urp_live/federation_live all false — Node0 carries the DNA, it does not activate the organism. Does not prove the injected evidence/monitor facts. No execution, daemon, network, token, wallet, mint, or federation. |
| Preview-only SAT-5 constitutional verifier set: five deterministic verifier passes (receipt/hash integrity, consent/FATE, impact/no-riba, security/blast-radius, governance/doctrine) that JUDGE a Node0 outcome — fail-closed admissibility, SAT judges Node0 and does not serve it, inert output with no authority, no mint, no live SAT agent. (SAT5-CONSTITUTIONAL-VERIFIER-SET-PREVIEW-1A) | `packages/core/src/sat5-constitutional-verifier-set-preview.js` + `tests/sat5-constitutional-verifier-set-preview.test.js` + `scripts/review/sat5-constitutional-verifier-set-preview-check.mjs` + `docs/02-architecture/SAT5_CONSTITUTIONAL_VERIFIER_SET_PREVIEW_v0_1.md` + `docs/receipts/SAT5_CONSTITUTIONAL_VERIFIER_SET_PREVIEW_1A.md`. [MEASURED] Exact-consent set of five constitutional verifier passes JUDGING a Node0 outcome: SAT-1 receipt/hash integrity, SAT-2 consent/FATE, SAT-3 impact/no-riba (mint/cost-as-value/simulated-as-real/unverified-impact fail), SAT-4 security/blast-radius (low passes; medium/high need reversible+backup), SAT-5 governance/doctrine (truth label + boundary all-false + no forbidden claims); fail-closed admissibility (FAIL or ABSTAIN rejects); judgment content-addressed + fully re-derivable so a forged ADMISSIBLE is rejected; constitutional stance judges_node0:true/serves_node0:false unflippable (20 tests). PREVIEW DESIGN of the judge, NOT live SAT autonomy — live_sat_agent:false, authority_delta:0, mint_allowed:false, urp_live:false; judges DECLARED facts, gathers/attests nothing, animates no agent. No execution, daemon, network, token, wallet, or federation. |
| Metadata-only Node0 homebase boundary kernel: hardware specs + OS-tree (host to VM/container to filesystem-root ownership) with inside/outside/unknown homebase classification; references existing device/data manifests instead of re-scanning or reading content. (NODE0-NODESPACE-BOUNDARY-PREVIEW-1A) | `packages/core/src/node0-nodespace-boundary-preview.js` + `tests/node0-nodespace-boundary-preview.test.js` + `scripts/review/node0-nodespace-boundary-preview-check.mjs` + `docs/02-architecture/NODE0_NODESPACE_BOUNDARY_PREVIEW_v0_1.md` + `docs/receipts/NODE0_NODESPACE_BOUNDARY_PREVIEW_1A.md`. [MEASURED] 52 focused tests + review gate green; hardware-spec + OS-tree boundary layer, inside/outside/unknown counts re-derived from the primary arrays (forged-summary rejection), raw serials refused; user-selected scan-depth envelope (metadata_only default up to full_local_content_index) encoded per root + tamper-proof kernel constant — node owner is sole authority, content_read_allowed_now false, only receipts cross nodes by default. No content read, execution, daemon, network, token, wallet, or federation; boundary all-false, authority_delta 0. |
| Preview-only composer that binds existing Dema organs (pain-goal, mission, NodeSpace boundary, homebase, proposed task, receipt preview, monitor, absence queue, return review) into one fail-closed operator work-envelope; references organs, does not run them or execute tasks. (DEMA-ACTIVE-WORKLOOP-COMPOSER-PREVIEW-1A) | `packages/core/src/dema-active-workloop-composer-preview.js` + `tests/dema-active-workloop-composer-preview.test.js` + `scripts/review/dema-active-workloop-composer-preview-check.mjs` + `docs/02-architecture/DEMA_ACTIVE_WORKLOOP_COMPOSER_PREVIEW_v0_1.md` + `docs/receipts/DEMA_ACTIVE_WORKLOOP_COMPOSER_PREVIEW_1A.md`. [MEASURED] 27 focused tests + review gate green; composes the shipped organs by reference into one fail-closed operator work-envelope (missing boundary/consent/receipt-preview, monitor critical, or irreversible file action all block; L3+ needs approval; absent→absence-queue candidate; return→return-review candidate); allowed_next_action re-derived from state (forged envelope rejected). No organ run, task execution, content read, daemon, network, token, wallet, or federation; boundary all-false, authority_delta 0. |
| Consent-scoped, metadata-only inventory summary kernel: derives a triage (categories, total bytes, stale/duplicate-name/sensitive-name candidates, largest) from injected file-metadata rows under a user-selected scan mode; metadata_only implemented, all five scan modes as future user options; no content read. (NODE0-CONSENTED-INVENTORY-GATHERER-PREVIEW-1A) | `packages/core/src/node0-consented-inventory-gatherer-preview.js` + `tests/node0-consented-inventory-gatherer-preview.test.js` + `scripts/review/node0-consented-inventory-gatherer-preview-check.mjs` + `docs/02-architecture/NODE0_CONSENTED_INVENTORY_GATHERER_PREVIEW_v0_1.md` + `docs/receipts/NODE0_CONSENTED_INVENTORY_GATHERER_PREVIEW_1A.md`. [MEASURED] 17 focused tests + review gate green; derives a workspace triage (categories, total bytes, largest, stale / duplicate-name / sensitive-name candidates, safe next actions) from injected file METADATA only; scan depth is the node owner's choice (metadata_only implemented, other four modes refused here); content-read claim refused; category counts internally consistent (forged-summary rejection). No content read, execution, daemon, network, upload, token, wallet, or federation; boundary all-false, authority_delta 0. Read-only metadata collection lives in a consent-scoped effect adapter over an operator-named root (not this kernel). |
| Self-eval quality baseline + compare: captures measured system-quality signals (tests, coverage, registry, monitor, gates, perf) as a content-addressed baseline and compares a candidate against it per dimension to say improved / regressed / mixed / unchanged, so system change is measured not blind; signals are injected, no tests are run here. (DEMA-SELF-EVAL-BASELINE-PREVIEW-1A) | `packages/core/src/dema-self-eval-baseline-preview.js` + `tests/dema-self-eval-baseline-preview.test.js` + `scripts/review/dema-self-eval-baseline-preview-check.mjs` + `docs/02-architecture/DEMA_SELF_EVAL_BASELINE_PREVIEW_v0_1.md` + `docs/receipts/DEMA_SELF_EVAL_BASELINE_PREVIEW_1A.md`. [MEASURED] 16 focused tests + review gate green; captures measured system-quality signals as a content-addressed baseline and compares candidate-vs-baseline per dimension into improved / regressed / unchanged (hard regression on fewer passing tests / dropped coverage / new monitor critical / gates green->red forces 'regressed'); signals injected (runs no tests/coverage/monitor), forged 'healthy' rejected by re-derivation. Measures change; does not itself improve Dema. No content read, execution, daemon, network, token, wallet, or federation; boundary all-false, authority_delta 0. |
| Preview-only verified-answer receipt cache: stores previously verified answers as content-addressed records and reuses them only when fresh, in-scope, source-hash-matched, and truth_label verified; a cache hit reuses proof, never grants action, never mints, never turns saved cost into value. (DEMA-VERIFIED-ANSWER-RECEIPT-CACHE-PREVIEW-1A) | `packages/core/src/dema-verified-answer-receipt-cache-preview.js` + `tests/dema-verified-answer-receipt-cache-preview.test.js` + `scripts/review/dema-verified-answer-receipt-cache-preview-check.mjs` + `docs/02-architecture/DEMA_VERIFIED_ANSWER_RECEIPT_CACHE_PREVIEW_v0_1.md` + `docs/receipts/DEMA_VERIFIED_ANSWER_RECEIPT_CACHE_PREVIEW_1A.md`. [MEASURED] 26 focused tests + review gate green; stores verified answers as content-addressed records and reuses them only when status `verified`, fresh (against an injected `now`), consent-scope matched (private scope needs a matching operator-consent token), and source-hash matched — every miss returns no hit; a hit reuses proof only (grants_action false, authority_delta 0). Superseded/rejected records never hit; tampered content_hash, non-zero authority_delta, vacuous/flipped boundary, and unknown status are all rejected by re-derivation. Integrity is body-bound content-addressing only — not signature-based tamper-resistance; the kernel reads no clock/network and does not mint or turn saved model cost into value. No execution, daemon, network, token, wallet, or federation; boundary all-false. |
| Isnād Modern design canon: a written, gate-enforced visual constitution for Dema's future TUI/GUI — proof-first, bilingual, zero-external-request, geometry-as-structure; the screen is a proof object. Docs-only, implements nothing. (DEMA-ISNAD-MODERN-TUI-DESIGN-CANON-1A) | `docs/design/DEMA_ISNAD_MODERN_DESIGN_SYSTEM_v0_1.md` + `docs/design/DEMA_TUI_HOMEBASE_BLUEPRINT_v0_1.md` + `scripts/review/dema-isnad-modern-design-canon-check.mjs` + `tests/dema-isnad-modern-design-canon.test.js` + `docs/receipts/DEMA_ISNAD_MODERN_TUI_DESIGN_CANON_1A.md`. [DOCS-ONLY] 7 focused tests + review gate green; gate requires 8 doctrine markers (gold-only-where-proof, every-number-chipped, Arabic-first-class, consent/errors/proof never mysterious/hidden, zero-external-request, docs-only) + 7 cockpit UX contracts, and forbids any affirmative live-runtime / GUI-TUI-implemented / mint / URP / federation claim. NOT in the capability truth registry (registry stays 36) — a design doctrine is not a measured code capability. No runtime, no implementation, no mint, no URP, no federation. |
| First Light front door: a self-contained, zero-external-request static GUI preview rendered from a pure contract kernel — bilingual (Arabic first-class), consent-first, evidence-chipped, PREVIEW_ONLY. Renders the contract; the shipped HTML is gate-verified against it. (DEMA-FIRST-LIGHT-GUI-FRONT-DOOR-PREVIEW-1A) | `packages/core/src/dema-first-light-front-door-preview.js` + `apps/front-door/index.html` + `tests/dema-first-light-front-door-preview.test.js` + `scripts/review/dema-first-light-front-door-preview-check.mjs` + `docs/02-architecture/DEMA_FIRST_LIGHT_GUI_FRONT_DOOR_PREVIEW_v0_1.md` + `docs/receipts/DEMA_FIRST_LIGHT_GUI_FRONT_DOOR_PREVIEW_1A.md`. [MEASURED] 13 focused tests + review gate green; the kernel contract is the source of truth and the HTML is verified against it — PREVIEW ONLY / NO MINT / NO FEDERATION banner, zero external requests (only opt-in `127.0.0.1` probe), URP/apps/data/daemon/receipt-mint labeled DESIGNED — NOT LIVE, apps/data toggles agent-gated, bond fingerprint (hash) exported not raw identity. Gate rejects external requests, non-localhost fetch, URP-ACTIVE, live mint/federation, missing disclaimer, or missing Arabic. Registry 36 → 37. No runtime, daemon, network (beyond opt-in localhost), token, wallet, or federation; boundary all-false, authority_delta 0. |
| Socratic critic (process supervision): a constraint-enforcing critic that interrogates a proposed hypothesis before SAT — asks "what would make this false?", raises question pressure only, never grants authority or claims truth. PAT proposes → critic interrogates → SAT verifies → receipt records. (DEMA-SOCRATIC-CRITIC-PROCESS-SUPERVISION-PREVIEW-1A) | `packages/core/src/dema-socratic-critic-process-supervision-preview.js` + `tests/dema-socratic-critic-process-supervision-preview.test.js` + `scripts/review/dema-socratic-critic-process-supervision-preview-check.mjs` + `docs/02-architecture/DEMA_SOCRATIC_CRITIC_PROCESS_SUPERVISION_PREVIEW_v0_1.md` + `docs/receipts/DEMA_SOCRATIC_CRITIC_PROCESS_SUPERVISION_PREVIEW_1A.md`. [MEASURED] 13 focused tests + review gate green; runs a hypothesis packet through the four Socratic gates as seven deterministic checks (clarification, constraint, causal-path, counterexample, falsification, uncertainty, verified-vs-inferred split) and hands off with one status: ready_for_sat / needs_revision / blocked_by_missing_evidence / rejected_overclaim. Violated constraint or certainty-outruns-evidence → rejected_overclaim; missing causal path/falsifier/evidence → blocked; vacuous claim → needs_revision. Content-addressed; grants_action false, claims_truth false, authority_delta 0, boundary all-false (verify rejects a grants_action tamper). Does not verify the claim (SAT's role), grant authority, execute, invoke a model, or touch the network. Registry 37 → 38. No runtime, daemon, network, token, wallet, or federation. |
| Zero-overclaim response policy: a deterministic answer discipline that classifies each claim, enforces an honest label (VERIFIED / INFERRED / SPECULATIVE / UNVERIFIED / BLOCKED_PENDING_EVIDENCE), blocks unsupported current/high-stakes answers, and refuses invented sources and authority inflation. Downgrades and blocks; never upgrades authority. (DEMA-ZERO-OVERCLAIM-RESPONSE-POLICY-1A) | `packages/core/src/dema-zero-overclaim-response-policy.js` + `tests/dema-zero-overclaim-response-policy.test.js` + `scripts/review/dema-zero-overclaim-response-policy-check.mjs` + `docs/02-architecture/DEMA_ZERO_OVERCLAIM_RESPONSE_POLICY_v0_1.md` + `docs/receipts/DEMA_ZERO_OVERCLAIM_RESPONSE_POLICY_1A.md`. [MEASURED] 14 focused tests + review gate green; classifies each claim and hands off with one status (cleared_to_respond / blocked_pending_evidence / rejected_overclaim). Unsupported fact → UNVERIFIED; current or high-stakes without evidence → BLOCKED_PENDING_EVIDENCE; invented source, inference/speculation asserted as VERIFIED, grants_action/authority_delta inflation, or claims_truth without a verified claim → rejected_overclaim. Content-addressed and stable; grants_action false, claims_truth false, authority_delta 0, boundary all-false (verify rejects a grants_action tamper). Does not verify truth, fetch evidence, invoke a model, or touch the network. Registry 38 → 39. No runtime, daemon, network, token, wallet, or federation. |
| URP supply-side resource reward contract (PREVIEW): encodes the public-market law — a provider earns base value from VERIFIED supply/availability/service, not from proving impact; the impact dividend is extra and requires a verified outcome. Computes reward-type eligibility previews; mints nothing, activates no live URP. Cost measured is not impact. (URP-SUPPLY-SIDE-RESOURCE-REWARD-CONTRACT-PREVIEW-1A) | `packages/core/src/urp-supply-side-resource-reward-contract-preview.js` + `tests/urp-supply-side-resource-reward-contract-preview.test.js` + `scripts/review/urp-supply-side-resource-reward-contract-preview-check.mjs` + `docs/02-architecture/URP_SUPPLY_SIDE_RESOURCE_REWARD_CONTRACT_PREVIEW_v0_1.md` + `docs/receipts/URP_SUPPLY_SIDE_RESOURCE_REWARD_CONTRACT_PREVIEW_1A.md`. [PREVIEW_ONLY] 17 focused tests + review gate green; evaluates a resource offer into reward-type eligibility (verified_supply / verified_availability / verified_usage / optional_impact_dividend) and one status (reward_preview_allowed / blocked_pending_consent / blocked_pending_measurement / blocked_pending_sat_audit / rejected_overclaim / rejected_policy_violation). Missing consent/measurement blocks; high-value needs a SAT audit ref; claimed impact without verified outcome blocks; policy violation, self-mint/live-URP/wallet/federation/authority-increase, cost-as-impact, or supply-reward-mislabeled-as-impact reject. Content-addressed; boundary all-false, authority_delta 0, grants_action false, **mint_allowed false** (verify rejects a mint_allowed tamper). Does not activate live URP, mint, settle, pay, federate, invoke a model, or touch the network — settlement + real impact remain DESIGNED_NOT_LIVE. Registry 39 → 40. |
| Preview-only signed receipt anchor: Ed25519 signature over a canonical-JSON receipt payload with injected keys; verifies signer identity and rejects payload/signature/canonicalization tamper and forge-and-recompute laundering. Mints nothing, binds no live identity. (DEMA-RECEIPT-SIGNATURE-ANCHOR-PREVIEW-1A) | `packages/core/src/dema-receipt-signature-anchor-preview.js` + `tests/dema-receipt-signature-anchor-preview.test.js` + `scripts/review/dema-receipt-signature-anchor-preview-check.mjs` + `docs/02-architecture/DEMA_RECEIPT_SIGNATURE_ANCHOR_PREVIEW_v0_1.md` + `docs/receipts/DEMA_RECEIPT_SIGNATURE_ANCHOR_PREVIEW_1A.md`. [MEASURED] 13 focused tests + review gate green; signs the whole canonical-JSON envelope body with an injected Ed25519 key and verifies against a trusted public key. Rejects payload/signature/canonicalization-drift tamper, signer mismatch, non-zero authority_delta, grants_action/mint_allowed true, flipped boundary, and unsigned-as-signed. **Rejects forge-and-recompute laundering** — the independent anchor the #334 content-addressed cache lacked (re-signing needs the private key). Keys are ephemeral/injected: binds no live Node0 genesis identity, persists/manages no keys. Boundary all-false, authority_delta 0, grants_action false, mint_allowed false. No execution, daemon, network, wallet, mint, or federation. |
| Preview-only Node0 URP Genesis Root: composes and validates a local resource-registry descriptor (identity, machine/compute/data resource policies, consent scopes, signed receipt-chain-head anchor, boundary flags) declaring what Node0 owns/permits/shares. Validates a caller-provided descriptor, activates nothing, tops at local_preview_active below the gated activate rung. Mints nothing, binds no live identity. (NODE0-URP-GENESIS-ROOT-ACTIVATION-PREVIEW-1A) | `packages/core/src/node0-urp-genesis-root-activation-preview.js` + `tests/node0-urp-genesis-root-activation-preview.test.js` + `scripts/review/node0-urp-genesis-root-activation-preview-check.mjs` + `docs/02-architecture/NODE0_URP_GENESIS_ROOT_ACTIVATION_PREVIEW_v0_1.md` + `docs/receipts/NODE0_URP_GENESIS_ROOT_ACTIVATION_PREVIEW_1A.md`. [MEASURED] 32 focused tests + review gate green. Composes a local resource-registry descriptor + signature-backed receipt-chain-head anchor (node0-signed-chain-head, injected key); reaches local_preview_active only when all required fields present AND every domain flag false; else blocked_pending_health/consent/resource_policy/data_policy or rejected_overclaim. Rejects live_urp/mint/wallet/settlement/federation/remote_execution/daemon flags, public-market/simulated-impact/cost-as-value wording, authority_delta>0, grants_action, naive tamper, anchor tamper. **Activates nothing** — sits below the gated activate rung; binds no live identity; first implementation of the reserved BIZRA_URP_GENESIS_PREVIEW slot. No execution, daemon, network, mint, wallet, settlement, or federation. |
| [MEASURED] Pure preview-only gate binding a Node0 URP genesis-root descriptor to existing URP resource-family preview surfaces under all-false boundary rules; activates nothing, mints nothing. (NODE0-URP-GENESIS-ROOT-COMPOSITION-GATE-PREVIEW-1A) | `packages/core/src/node0-urp-genesis-root-composition-gate-preview.js` + `tests/node0-urp-genesis-root-composition-gate-preview.test.js` + `scripts/review/node0-urp-genesis-root-composition-gate-preview-check.mjs` + `docs/02-architecture/NODE0_URP_GENESIS_ROOT_COMPOSITION_GATE_PREVIEW_v0_1.md` + `docs/receipts/NODE0_URP_GENESIS_ROOT_COMPOSITION_GATE_PREVIEW_1A.md`. 27 focused tests + review gate green. Deterministically decides whether a signature-verified, local_preview_active genesis-root descriptor may compose with a declared set of known URP resource-family surfaces (each drift-guarded to a real kernel schema, all-false boundary, unpublished, preview-only settlement, non-minting, non-cost-as-impact, no-raw-data); overclaim or authority_delta>0 fails closed; verdict content-addressed with the embedded genesis anchor defeating forge-and-recompute. Validates caller-normalized surface attestations — runs no resource kernel. No execution, daemon, network, token, wallet, settlement, or federation; boundary all-false, authority_delta 0, mint_allowed false. |
| [MEASURED] Pure preview-only Node0 mission pulse: connects one caller-supplied mission packet through the eight-stage pulse (perceive → consent → resource-composition reference → action preview → verify → receipt preview → world-state delta preview → DEMA truth report) — all boundary-false, activates nothing. (NODE0-FIRST-REAL-LOCAL-MISSION-PULSE-PREVIEW-1A) | `packages/core/src/node0-first-real-local-mission-pulse-preview.js` + `tests/node0-first-real-local-mission-pulse-preview.test.js` + `scripts/review/node0-first-real-local-mission-pulse-preview-check.mjs` + `docs/02-architecture/NODE0_FIRST_REAL_LOCAL_MISSION_PULSE_PREVIEW_v0_1.md` + `docs/receipts/NODE0_FIRST_REAL_LOCAL_MISSION_PULSE_PREVIEW_1A.md`. 32 focused tests + review gate green. First control-plane bridge from composed architecture toward a living loop: validates one caller-supplied mission over a re-verified composition reference (signature-backed genesis anchor), fails closed on live-mutation consent / invalid composition / overclaiming candidate / any declared live-capability flag / authority_delta>0 / live-commit request; RECEIPT + WORLD_STATE are previews (committed_live false); ACTION_PREVIEW validates a caller-supplied candidate's shape, performs no semantic extraction, reads no file. No execution, daemon, network, token, wallet, mint, model invocation, or federation; boundary all-false, authority_delta 0. |
| [MEASURED] Operator-invoked local mission harness: `dema mission pulse <file>` reads one explicitly-named local file read-only (metadata + hash; a bounded excerpt only under a separate exact excerpt-consent), builds a mission packet, runs the pure mission-pulse kernel over a composition reference, and shapes a preview receipt artifact written only with --receipt + consent (atomic, under $DEMA_HOME) — fs confined to the CLI/adapter, kernel stays pure, no daemon, no network, no model, no source mutation. (NODE0-LOCAL-MISSION-HARNESS-PREVIEW-1A) | `packages/core/src/node0-local-mission-harness-preview.js` + `apps/cli/src/commands/mission.js` (pulse subcommand) + `tests/node0-local-mission-harness-preview.test.js` + `tests/node0-local-mission-harness-cli.test.js` + `scripts/review/node0-local-mission-harness-preview-check.mjs` + `docs/02-architecture/NODE0_LOCAL_MISSION_HARNESS_PREVIEW_v0_1.md` + `docs/receipts/NODE0_LOCAL_MISSION_HARNESS_PREVIEW_1A.md`. 21 kernel tests + 11 CLI/adapter tests + review gate green. First I/O boundary crossing: pure kernel over an injected file-ref + operator-supplied candidate (NO semantic extraction), embedded pulse verdict re-verifies (→ composition → genesis signature); adapter reads exactly one file read-only, excerpt needs separate consent, receipt written only with consent (committed_live false), source never mutated. No execution, daemon, network, token, wallet, mint, model invocation, or federation; boundary all-false, authority_delta 0. |
| [MEASURED] Pure preview-only return-review that closes the mission loop's READ side: `dema mission review <receipt>` reads a `dema mission pulse` receipt (read-only), a pure kernel verifies its structure + preview invariants, states what was proven and what was NOT (semantic correctness not judged; no chain re-derivation from the summary), and recommends exactly one next safe action; no model/network/daemon, kernel stays pure. (NODE0-MISSION-HARNESS-RETURN-REVIEW-PREVIEW-1A) | `packages/core/src/node0-mission-harness-return-review-preview.js` + `apps/cli/src/commands/mission.js` (review subcommand) + `tests/node0-mission-harness-return-review-preview.test.js` + `tests/node0-mission-harness-return-review-cli.test.js` + `scripts/review/node0-mission-harness-return-review-preview-check.mjs` + `docs/02-architecture/NODE0_MISSION_HARNESS_RETURN_REVIEW_PREVIEW_v0_1.md` + `docs/receipts/NODE0_MISSION_HARNESS_RETURN_REVIEW_PREVIEW_1A.md`. 18 kernel tests + 5 CLI/adapter tests + review gate green. Completes the loop `file → hash → mission packet → pulse → preview receipt → return review`; reviewing a bad receipt is the kernel's job (run completes, receipt_ok false); verify rejects ok-without-proof / not-ok-but-claims-proof forgeries. No execution, daemon, network, token, wallet, mint, model invocation, or federation; boundary all-false, authority_delta 0. |
| [MEASURED] Pure preview-only local URP shelf index — the first House-of-Wisdom shelf: `dema mission shelf` reads every `dema mission pulse` receipt under $DEMA_HOME/mission/receipts (read-only) and a pure kernel composes them into a deterministic, content-addressed queryable catalog (mission ids, file/pulse hashes, per-receipt review status, valid/invalid/live-leak counts) so the write-only receipts become READABLE (URP_LOCAL_ACTIVE you can ASK). Commits no live world-state; publishes nothing to any shared/federated URP; kernel stays pure. (NODE0-LOCAL-URP-SHELF-INDEX-PREVIEW-1A) | `packages/core/src/node0-local-urp-shelf-index-preview.js` + `apps/cli/src/commands/mission.js` (shelf subcommand) + `tests/node0-local-urp-shelf-index-preview.test.js` + `tests/node0-local-urp-shelf-index-cli.test.js` + `scripts/review/node0-local-urp-shelf-index-preview-check.mjs` + `docs/02-architecture/NODE0_LOCAL_URP_SHELF_INDEX_PREVIEW_v0_1.md` + `docs/receipts/NODE0_LOCAL_URP_SHELF_INDEX_PREVIEW_1A.md`. 19 kernel tests + 4 CLI/adapter tests + review gate green. Entries order-independent (stable content hash); a bad receipt is catalogued but counted invalid; a committed_live receipt is surfaced as a live_leak; verify re-derives every count so a forged count is rejected; reuses the return-review validator. No execution, daemon, network, token, wallet, mint, model invocation, or federation; boundary all-false, authority_delta 0. |
| [MEASURED] Pure preview-only receipt-shelf compaction — compact PROOF, not prose: `dema mission compact` reads receipts → shelf → a pure kernel re-verifies the shelf and compacts it into a hash-bound mission state that RETAINS only verified signals (mission ids, file/pulse hashes, review status, counts) and EXPLICITLY declares what was DROPPED (raw content, unverified semantic claims, model-generated meaning), what can no longer be claimed, and exactly one next safe action; no RL, no model, no network, no live URP write, kernel stays pure. (NODE0-RECEIPT-SHELF-COMPACTION-STATE-PREVIEW-1A) | `packages/core/src/node0-receipt-shelf-compaction-state-preview.js` + `apps/cli/src/commands/mission.js` (compact subcommand) + `tests/node0-receipt-shelf-compaction-state-preview.test.js` + `tests/node0-receipt-shelf-compaction-cli.test.js` + `scripts/review/node0-receipt-shelf-compaction-state-preview-check.mjs` + `docs/02-architecture/NODE0_RECEIPT_SHELF_COMPACTION_STATE_PREVIEW_v0_1.md` + `docs/receipts/NODE0_RECEIPT_SHELF_COMPACTION_STATE_PREVIEW_1A.md`. 18 kernel tests + 3 CLI/adapter tests + review gate green. The Ihsān gate (keep/drop/no-longer-claim/one-next-action) is answered in full; verify rejects a compaction that dropped its own dropped-list and re-derives every count from the embedded shelf (launder chain compaction→shelf→receipts, content-addressing only — cannot re-derive the genesis signature from summaries). live_leak → next action is quarantine not act. No execution, daemon, network, token, wallet, mint, model invocation, or federation; boundary all-false, authority_delta 0, committed_live false. |
| [MEASURED] Pure preview-only **Layer -1 corpus safety gate** — the guard before any untrusted text touches memory/RAG. `dema corpus sanitize --file <abs>` reads a file read-only; a pure kernel deterministically scans (regex/lexicon, no model) for secret-like strings, prompt-injection payloads, and authority-escalation, and emits a content-addressed verdict — active injection/authority → BLOCKED, secrets alone → QUARANTINED (redacted), clean → ALLOWED. Secrets are never echoed in full; verify rejects a leak or a forged verdict; ingest_performed always false; CLI exits non-zero unless ALLOWED (pipeline gate). (UNTRUSTED-CORPUS-SANITIZER-PREVIEW-1A) | `packages/core/src/untrusted-corpus-sanitizer-preview.js` + `apps/cli/src/commands/corpus.js` (sanitize subcommand) + `tests/untrusted-corpus-sanitizer-preview.test.js` + `tests/untrusted-corpus-sanitizer-cli.test.js` + `scripts/review/untrusted-corpus-sanitizer-preview-check.mjs` + `docs/02-architecture/UNTRUSTED_CORPUS_SANITIZER_PREVIEW_v0_1.md` + `docs/receipts/UNTRUSTED_CORPUS_SANITIZER_PREVIEW_1A.md`. 16 kernel tests + 4 CLI tests + review gate green (fixture = the real pasted-transcript attack → BLOCKED). It is a pattern FILTER not a safety proof (cannot catch novel/obfuscated attacks); ALLOWED ≠ semantically safe; QUARANTINED needs human/SAT review. No execution, daemon, network, token, wallet, mint, model invocation, federation, or ingestion; boundary all-false, authority_delta 0. |
| [MEASURED] Pure preview-only **public-metric claim-binding gate** (Materialization Pulse Step 5) — the output-side truth guard that pairs with the input-side sanitizer. Binds each structured public claim to an injected evidence store by hierarchy (signed_receipt > ci_attestation > current_limits > claim_ledger > repo_state > operator_declaration; ai_text never authority) via an EXACT value check → VERIFIED / DERIVED / DECLARED / PREVIEW / UNKNOWN / REJECTED / REMOVED. Shape-matching RECOGNIZES; only evidence PROVES. Reproduces the containment acceptance set: '12,680 tests'→REJECTED, '6,993 Dema-core'→VERIFIED, '~15,000 hrs'→DECLARED, 'Live URP'/'SEED minted'→REJECTED, 'URP Preview'→PREVIEW, wrong-value→REJECTED, no-evidence→UNKNOWN. Only VERIFIED/DERIVED/DECLARED/PREVIEW are public-displayable; verify re-derives every binding (a laundered label is rejected). (PUBLIC-METRIC-CLAIM-GATE-PREVIEW-1A) | `packages/core/src/public-metric-claim-gate-preview.js` + `tests/public-metric-claim-gate-preview.test.js` + `scripts/review/public-metric-claim-gate-preview-check.mjs` + `docs/02-architecture/PUBLIC_METRIC_CLAIM_GATE_PREVIEW_v0_1.md` + `docs/receipts/PUBLIC_METRIC_CLAIM_GATE_PREVIEW_1A.md`. 20 tests + review gate green. Does NOT extract claims from raw copy (claims supplied structured) or measure evidence itself (injected); isomorphism is recognition-only, never truth. No execution, daemon, network, token, wallet, mint, model, federation, or deploy; boundary all-false, authority_delta 0. |
| [MEASURED] Pure preview-only **canonical Materialization Pulse receipt envelope** — the "missing middle" binding both truth membranes into one atomic, content-addressed transaction. Assembles a receipt (`bizra.materialization_pulse_receipt.v0.1`) linking niyyah, input_safety (corpus-sanitizer verdict), plan, FATE verdict, execution mode, and claim_binding (claim-gate verdict) under a 6-key all-false Pulse boundary + explicit does_not_prove. Enforces atomicity: sealed pulse must reference BOTH membranes; sealed-over-BLOCKED-input is illegal (aborted-only); claims_public_safe requires zero REJECTED claims; mint/federation/authority_delta violations rejected; does_not_prove must include live_urp/mint/federation. verify re-derives the receipt eval (forged receipt_ok rejected); a malformed pulse still runs with receipt_ok false. (MATERIALIZATION-PULSE-RECEIPT-SCHEMA-PREVIEW-1A) | `packages/core/src/materialization-pulse-receipt-schema-preview.js` + `tests/materialization-pulse-receipt-schema-preview.test.js` + `scripts/review/materialization-pulse-receipt-schema-preview-check.mjs` + `docs/02-architecture/MATERIALIZATION_PULSE_RECEIPT_SCHEMA_PREVIEW_v0_1.md` + `docs/receipts/MATERIALIZATION_PULSE_RECEIPT_SCHEMA_PREVIEW_1A.md`. 18 tests (incl. all 10 acceptance criteria) + review gate green. It RUNS no pulse and re-runs no sub-gate — sub-receipts are injected hashes it BINDS, not payloads it re-verifies. No execution, daemon, network, token, wallet, mint, model, or federation; boundary all-false, authority_delta 0. |
| [MEASURED] Pure preview-only **local-model Pulse binding gate** — the mind→Pulse bridge. Binds an already-produced `bizra.dema.llm_invocation_result.v0.1` envelope into the Materialization Pulse evidence lane as SUGGESTION-ONLY data (the model may suggest; the verifier remains authority). Invokes NO model. Completed invocation admissible only when prompt+response verdicts are `PUBLIC_SAFE` + verdict role is suggestion; blocked/failed invocations recordable as failure evidence only. verify rejects unsafe prompt/response, non-suggestion roles, runtime strict-false boundary violations, wrong source schema, admissible/failure contradiction, and public-claim/action/mint/wallet/federation/authority laundering even with a recomputed hash; run() self-probes forgeries. Reuses canonical buildPreviewBoundary(); input fixtures live in scripts/review to keep the boundary-invariant static check clean. (LOCAL-MODEL-PULSE-BINDING-PREVIEW-1A) | `packages/core/src/local-model-pulse-binding-preview.js` + `tests/local-model-pulse-binding-preview.test.js` + `scripts/review/local-model-pulse-binding-preview-check.mjs` + `scripts/review/local-model-pulse-binding-fixtures.mjs` + `docs/02-architecture/LOCAL_MODEL_PULSE_BINDING_PREVIEW_v0_1.md` + `docs/receipts/LOCAL_MODEL_PULSE_BINDING_PREVIEW_1A.md`. 14 tests + review gate green. Does NOT invoke a model, call Ollama, use network, verify semantic truth, authorize action, publish claims, mint, use wallet, federate, or prove live URP; boundary all-false, authority_delta 0. |
| [MEASURED] Pure preview-only **plan-branch evidence gate** (Materialization Pulse planning layer, niyyah→FATE) — preserves candidate, chosen, and rejected plan branches as content-addressed evidence. **Rejected branches are evidence:** every non-chosen candidate must be accounted for with a valid rejection_reason (fixed set) + non-empty basis; duplicate/empty/ghost ids, missing/wrong chosen branch, chosen-and-rejected conflict, unaccounted branches, non-zero authority_delta, out-of-range risk/ihsan scores, action/authority/mint/wallet/federation/model laundering (recomputed-hash), and boundary flips are rejected; run() self-probes forgeries. (PLAN-BRANCH-PREVIEW-1A) | `packages/core/src/plan-branch-preview.js` + `tests/plan-branch-preview.test.js` + `scripts/review/plan-branch-preview-check.mjs` + `scripts/review/plan-branch-preview-fixtures.mjs` + `docs/02-architecture/PLAN_BRANCH_PREVIEW_v0_1.md` + `docs/receipts/PLAN_BRANCH_PREVIEW_1A.md`. 21 tests + review gate green. Does not execute, invoke a model, authorize action, verify external truth, mint, use wallet, federate, or prove live URP; boundary all-false, authority_delta 0. |
| [MEASURED] Pure preview-only **Materialization Pulse E2E orchestrator** — the capstone: it makes the assembled Pulse RUN. `dema mission run <file>` reads one real file read-only and runs it end-to-end through the five station kernels (sanitize → plan-branch → FATE → claim-gate → pulse-receipt envelope), emitting a per-station ladder + one chained receipt. Composes existing pure kernels; invokes NO model. Only ALLOWED input proceeds (BLOCKED/QUARANTINED abort @1); failed plan / FATE reject abort at their rung; claim rejection sets claims_public_safe:false without aborting; all-pass seals. verify re-derives the hash, re-verifies the embedded envelope, and cross-checks ladder-vs-envelope hashes (forged rung / laundered authority rejected). (NODE0-MATERIALIZATION-PULSE-E2E-PREVIEW-1A) | `packages/core/src/node0-materialization-pulse-e2e-preview.js` + `apps/cli/src/commands/mission.js` (run subcommand) + `tests/node0-materialization-pulse-e2e-preview.test.js` + `tests/node0-materialization-pulse-e2e-cli.test.js` + `scripts/review/node0-materialization-pulse-e2e-preview-check.mjs` + `scripts/review/materialization-pulse-e2e-fixtures.mjs` + `docs/02-architecture/NODE0_MATERIALIZATION_PULSE_E2E_PREVIEW_v0_1.md` + `docs/receipts/NODE0_MATERIALIZATION_PULSE_E2E_PREVIEW_1A.md`. 27 kernel + 4 CLI tests + review gate green. Runs no live model, executes no real-world action, publishes/mints nothing; a sealed pulse means the PREVIEW stations passed, NOT that the mission ran or claims are true; boundary all-false, authority_delta 0. |
| [MEASURED] Pure preview-only **local-mission artifact emitter/serializer** — re-verifies an already-produced NODE0-LOCAL-MISSION-HARNESS-PREVIEW result (transitively re-verifying pulse → composition → signature-backed genesis anchor) and serializes it into THREE separate content-addressed preview artifacts: a receipt, a not-applied world-state delta preview (applied:false), and a DEMA report. Each artifact carries a stable sha256 hash, committed_live:false, and an all-false boundary; the run id + target relpaths derive deterministically from the input content hash (same input → identical run id + artifact hashes). verify re-derives the emission hash AND each artifact hash and fail-closed rejects tamper, committed_live/authority/mint/laundering flags, raw-source-content leakage, and a forge-and-recompute where the embedded harness anchor no longer verifies. Composes the shipped harness kernel; re-implements nothing; writes no file (a CLI/adapter would, consent-gated, under DEMA_HOME). (NODE0-LOCAL-MISSION-ARTIFACT-EMISSION-PREVIEW-1A) | `packages/core/src/node0-local-mission-artifact-emission-preview.js` + `tests/node0-local-mission-artifact-emission-preview.test.js` + `scripts/review/node0-local-mission-artifact-emission-preview-check.mjs` + `docs/02-architecture/NODE0_LOCAL_MISSION_ARTIFACT_EMISSION_PREVIEW_v0_1.md` + `docs/receipts/NODE0_LOCAL_MISSION_ARTIFACT_EMISSION_PREVIEW_1A.md`. 39 kernel tests + review gate green. Serializing a preview is not executing a mission: no world-state applied, nothing recorded live, no model/network/daemon/wallet/mint/federation; boundary all-false, authority_delta 0. |
| [MEASURED] Pure preview-only **read-only mission cockpit / truth viewer** — takes ONE injected emission result (the three content-addressed artifacts: receipt, not-applied world-state delta, DEMA report), re-verifies it (transitively harness → pulse → composition → signature-backed genesis anchor), independently re-derives EACH artifact's content hash, refuses any tampered artifact (per-artifact), and renders one content-addressed operator cockpit view: mission status, accepted/rejected pulse gates (+ furthest reached station), the applied:false world-state delta summary, the DEMA report (status + next safe action), and the what-happened / what-did-not-happen / next-safe-action operator lines. Deterministic for the same input; verify binds the view to the source and rejects committed_live/authority/mint/boundary laundering and a broken upstream anchor. No new intelligence — it re-derives no artifact from the harness and asserts nothing the artifacts do not already state. (NODE0-MISSION-PILOT-COCKPIT-PREVIEW-1A) | `packages/core/src/node0-mission-pilot-cockpit-preview.js` + `tests/node0-mission-pilot-cockpit-preview.test.js` + `scripts/review/node0-mission-pilot-cockpit-preview-check.mjs` + `docs/02-architecture/NODE0_MISSION_PILOT_COCKPIT_PREVIEW_v0_1.md` + `docs/receipts/NODE0_MISSION_PILOT_COCKPIT_PREVIEW_1A.md`. 54 kernel tests + review gate green. A rendered view means the artifacts are content-addressed and consistent and the anchor verifies — NOT that the mission ran or its claims are true; no execution, world-state applied, model, network, daemon, wallet, mint, or federation; boundary all-false, authority_delta 0. |
| [MEASURED] Consent-gated operator **write** CLI — **`dema mission emit <file>`** reads one explicit ABSOLUTE local file read-only (metadata + content hash; a bounded excerpt only under a separate exact consent), composes the shipped harness → artifact-emission preview path over an ephemeral (no-live-identity) composition reference, and — ONLY under the exact phrase `GO: node0 local mission emit` AND a verified emission — atomically writes three preview artifacts (receipt, world_state_delta_preview, dema_report) plus one verification envelope (emission.json) under `$DEMA_HOME/artifacts/proofs/node0-local-mission/<run_id>/` via tmp+rename at mode 0600. Each written artifact's content hash matches the kernel's; the source file is byte-identical after the run; fail-closed without the exact phrase (nothing written); nothing is written outside the run_id dir. emission.json wraps the untouched content-addressed emission (harness_result intact — the object the mission-pilot cockpit kernel re-verifies as input.emission) with convenience fields outside that hashed body, so the future `dema mission cockpit <run-id>` reader loads one file to re-verify the full chain and render the gates panel from disk; a round-trip test proves the on-disk envelope returns ok:true through the cockpit, and an exclusion scan proves it holds no private key, DID secret, wallet, or raw source content. This is the first realized write surface over the emission kernel (which itself writes nothing). (NODE0-LOCAL-MISSION-EMIT-CLI-ADAPTER-1A) | `apps/cli/src/commands/mission.js` (emit subcommand) + `tests/node0-local-mission-emit-cli-adapter.test.js` + `scripts/review/node0-local-mission-emit-cli-adapter-check.mjs` + `docs/02-architecture/NODE0_LOCAL_MISSION_EMIT_CLI_ADAPTER_v0_1.md` + `docs/receipts/NODE0_LOCAL_MISSION_EMIT_CLI_ADAPTER_1A.md`. 19 CLI tests + review gate green. Writing three preview artifacts plus a verification envelope is not executing a mission and applies no world-state (the delta is declared, applied:false); no live identity/DID persisted (ephemeral in-memory preview signature only), no model, network, daemon, wallet, mint, federation, or live URP; committed_live false, boundary all-false, authority_delta 0. |
| [MEASURED] **Read-only** operator truth cockpit CLI — **`dema mission cockpit <run-id>`** loads the on-disk `emission.json` verification envelope a prior `dema mission emit` wrote and renders one operator view of that run. It strict-validates `<run-id>` against `^[0-9a-f]{16}$` BEFORE building any path (rejecting `..`, `../x`, and non-hex ids as a path-traversal guard), reads ONLY the four files in the run_id dir (envelope + receipt/world_state_delta_preview/dema_report) with no directory crawl and no source-file read, feeds the envelope's nested content-addressed emission to the shipped cockpit kernel (which re-verifies emission → harness → pulse → composition → signature-backed genesis anchor and renders the gates ladder), and INDEPENDENTLY re-derives each on-disk artifact file's sha256 to refuse a tampered file (`artifact_hash_mismatch:<name>` — which the kernel over the untouched embedded emission alone misses) or a missing file. Renders mission status, run id, receipt hash, gates ladder + furthest reached station, the applied:false world-state delta summary, the DEMA report, and the what-happened / what-did-not-happen / next-safe-action lines; writes ZERO files (run dir file set byte-identical before and after). A tampered emission.json, a missing envelope, and an envelope with no nested emission are refused. This is the read companion to the emit writer. (NODE0-MISSION-PILOT-COCKPIT-CLI-ADAPTER-1A) | `apps/cli/src/commands/mission.js` (cockpit subcommand) + `tests/node0-mission-pilot-cockpit-cli-adapter.test.js` + `scripts/review/node0-mission-pilot-cockpit-cli-adapter-check.mjs` + `docs/02-architecture/NODE0_MISSION_PILOT_COCKPIT_CLI_ADAPTER_v0_1.md` + `docs/receipts/NODE0_MISSION_PILOT_COCKPIT_CLI_ADAPTER_1A.md`. 17 CLI tests + review gate green. Reading and rendering the emitted artifacts is not executing a mission and applies no world-state (the delta is declared, applied:false); a rendered view means the on-disk artifacts are content-addressed and consistent and the anchor verifies — NOT that the mission ran or its claims are true; no model, network, daemon, wallet, mint, federation, or live URP; committed_live false, boundary all-false, authority_delta 0. |
| [MEASURED] Pure preview-only **Sovereign Voice Turn** — the first lawful mouth layer. `dema voice turn <file>` reads one transcript text file read-only, runs the merged Materialization Pulse E2E preview over that text, and builds a voice-turn receipt binding transcript hash → Pulse result hash → deterministic spoken-response plan. If the Pulse seals, speech may describe sealed preview status; if the Pulse aborts, speech must be refusal/status language. verify rejects content-hash tamper, audio/STT/TTS/microphone/model/network laundering, action/authority/mint/wallet/federation laundering, boundary flips, sealed-pulse refusal-only speech, and aborted-pulse completion/execution language. (SOVEREIGN-VOICE-TURN-PREVIEW-1A) | `packages/core/src/sovereign-voice-turn-preview.js` + `apps/cli/src/commands/voice.js` + `tests/sovereign-voice-turn-preview.test.js` + `tests/sovereign-voice-turn-preview-cli.test.js` + `scripts/review/sovereign-voice-turn-preview-check.mjs` + `scripts/review/sovereign-voice-turn-preview-fixtures.mjs` + `docs/02-architecture/SOVEREIGN_VOICE_TURN_PREVIEW_v0_1.md` + `docs/receipts/SOVEREIGN_VOICE_TURN_PREVIEW_1A.md`. 26 kernel + 4 CLI tests + review gate green. Not live voice: no microphone, STT, TTS, audio generation/playback, model, network, action, mint, wallet, federation, or live URP; boundary all-false, authority_delta 0. |
| [MEASURED] LOCAL_ONLY candidate **founder-impact loop** — `dema founder impact scope\|run\|verify`. Composes the shipped untrusted-corpus sanitizer, the OKF founder-impact digest, the public-metric claim-gate, and the FDE dual diagnostic into ONE content-addressed CANDIDATE receipt that serves the founder. The exact phrase `GO: dema founder impact loop 0a` gates the run; each declared source in a manifest passes the sanitizer (a BLOCKED source aborts before any digest); the digest is OKF-conformant and binds SOURCE HASHES, never raw bytes; a REJECTED public claim refuses the receipt; the FDE dual diagnostic classifies a missing local model as an OUTWARD environment gap ADVISORY-ONLY. `impact_class` is `candidate`, `served_to` is `founder`, `mint_allowed` is false; the honest boundary declares content_read/filesystem_write_performed/consent_collected true and every runtime/model/network/mint/federation key false; verify re-derives the whole body and each embedded rail. CORE INVARIANT (verified by the review gate): no FDE classification can flip `mint_allowed` / `continue_allowed` / `scope_expansion_allowed` from false to true — a failure classification cannot increase authority. (NODE0-FOUNDER-IMPACT-LOOP-0A) | `packages/core/src/node0-founder-impact-loop-preview.js` + `packages/core/src/node0-founder-impact-digest.js` + `packages/core/src/node0-founder-impact-gather.js` + `apps/cli/src/commands/founder.js` + `tests/node0-founder-impact-loop.test.js` + `tests/node0-founder-impact-digest.test.js` + `scripts/review/node0-founder-impact-loop-check.mjs` + `docs/TESTING.md`. 17 loop + 7 digest tests + review gate green. This proves ONLY a local candidate loop: NOT live PoI, NOT verified impact, NOT token mint, NOT federation, NOT autonomous PAT/SAT, NOT RSI. It runs no model, opens no network, starts no daemon, includes no raw source bytes; the receipt is content-addressed, not Ed25519-signed; boundary honest (3 true / 14 false), authority_delta 0, mint_allowed false. |
| [MEASURED] Pure preview-only **SkillOpt edit ledger** — records a SkillOpt-style skill-document edit-optimization *attempt* as a content-addressed receipt and fails closed on authority expansion. Motivation only: arXiv:2605.23904 (SkillOpt, text-space bounded edits + held-out validation) — this kernel runs no optimizer. Distinct from `agent-skill-ledger.js` (AGENT-SKILL-1A = agent XP), this is skill-DOCUMENT edit attempts. Positive-validates the entry and rejects any claim of `authority_delta != 0`, a changed `boundary_unchanged`/`consent_unchanged`/`current_limits_unchanged` surface, an `accepted` edit with no cited held-out validation refs, or a rejected edit with no stated reason; body-bound `verify` re-derives the hash and re-checks invariants so a self-consistent-but-illegal receipt still fails; `run()` self-probes a forged `authority_delta`. Core law: skills may improve, authority may not self-expand. (DEMA-SKILLOPT-EDIT-LEDGER-PREVIEW-1A) | `packages/core/src/dema-skillopt-edit-ledger-preview.js` + `tests/dema-skillopt-edit-ledger-preview.test.js` + `scripts/review/dema-skillopt-edit-ledger-preview-check.mjs` + `docs/02-architecture/DEMA_SKILLOPT_EDIT_LEDGER_PREVIEW_v0_1.md` + `docs/receipts/DEMA_SKILLOPT_EDIT_LEDGER_PREVIEW_1A.md`. 17 tests + review gate green. Recording an attempt is not running it: does NOT run the SkillOpt optimizer, invoke a model, generate or apply a skill edit, promote any skill, or enforce strict held-out score improvement (a documented future invariant needing an independent held-out anchor); no execution, daemon, network, token, wallet, or federation; boundary all-false, authority_delta 0. |
| [MEASURED] Pure preview-only **root-bound consent envelope** — a content-addressed consent-context envelope + fail-closed validator that BINDS an exact consent phrase to the exact action context (proposal, payload, capability scope, root set, action class, nonce, expiry) so a valid phrase cannot be replayed against a different payload, mission, action class, or scope. `buildConsentContext` derives `phrase_hash` and `consent_context_hash` (never caller-supplied); `evaluateContextBoundConsent` returns verdict `BLOCK` on: a non-exact phrase, a replay against a different `proposal_hash`/`payload_hash`/`capability_scope_hash`/`root_set_hash`, an action-class mismatch (strict ladder equality — a `C1_READ` consent never authorizes a `C3_LOCAL_WRITE`), an expired consent (`now >= expires_at`), a reused nonce, a missing/empty envelope root set or required phrase, and any field mutated after the sealed `consent_context_hash`; a fully matching context with an unused nonce and `now < expires_at` returns `PERMIT_PREVIEW`. Only hashes are carried — no private key or raw root-document text — and smuggled input fields are dropped by the field whitelist. Core law: a consent phrase authorizes exactly one action context. (DEMA-ROOT-BOUND-CONSENT-ENVELOPE-PREVIEW-1A) | `packages/consent/src/root-bound-consent-envelope-preview.js` + `tests/root-bound-consent-envelope-preview.test.js` + `scripts/review/root-bound-consent-envelope-preview-check.mjs` + `docs/02-architecture/DEMA_ROOT_BOUND_CONSENT_ENVELOPE_PREVIEW_v0_1.md` + `docs/receipts/DEMA_ROOT_BOUND_CONSENT_ENVELOPE_PREVIEW_1A.md`. 22 tests + review gate green. It proves consent can be cryptographically bound to an exact action context and that replay/escalation is rejected; it does NOT prove a live root-clause trace registry (deferred), live governance/FATE-EffectCap runtime, or any live mutation; effects (`nonce`, `expires_at`, `now`) are injected and time is compared with `Date.parse` (a pure parse, not a clock read); no optimizer, model, network, daemon, token, wallet, federation, or identity binding; boundary all-false, authority_delta 0. |
| [MEASURED] Pure preview-only **root-clause trace registry** — the next layer above the root-bound consent envelope. A hand-reviewed registry of 12 clauses drawn from the Three-Root Canon (The Message / الرسالة, The Seed / البذرة, The Third Fact), each mapping `clause_id` → an honest one-sentence summary → `clause_hash` (sha256 of that summary), plus a pure kernel that derives a content-addressed `root_set_hash` from a selected clause set — the value the consent envelope consumes as its `root_set_hash`, so a consent binding points at named clauses instead of an opaque hash. The registry is INJECTED (no fs in the kernel; the review gate/tests read the JSON). `buildRootTrace({clause_ids, registry})` emits a deterministic, order-independent trace carrying only `clause_id`+`root`+`clause_hash` (never the raw summary). `verifyRootTrace({trace, registry})` returns verdict `BLOCK` on: an unknown clause (`unknown_clause`), a selection missing any of the three roots (`three_root_set_incomplete` — the core "all three roots must be represented" invariant), a `clause_hash` that does not match the registry's hash of that summary (`clause_hash_mismatch`), an empty clause set (`empty_clause_set`), and a recomputed `root_set_hash` differing from the carried one (`root_set_hash_mismatch`); a valid three-root selection returns `PERMIT_PREVIEW` with a stable `root_set_hash`. The 12 summaries are hand-reviewed PARAPHRASES — not the authoritative encoding of the roots; the sealed root PDFs under `docs/root-canon/` (pinned by `docs/root-canon/root-canon.manifest.json`) remain canonical. Core law: a valid root trace must carry at least one clause from each of the three roots and every clause_hash must match. (DEMA-ROOT-CLAUSE-TRACE-REGISTRY-PREVIEW-1A) | `packages/consent/src/root-clause-trace-preview.js` + `docs/canon/BIZRA_ROOT_CLAUSE_REGISTRY_v0_1.json` + `tests/root-clause-trace-preview.test.js` + `scripts/review/root-clause-trace-preview-check.mjs` + `docs/02-architecture/DEMA_ROOT_CLAUSE_TRACE_REGISTRY_PREVIEW_v0_1.md` + `docs/receipts/DEMA_ROOT_CLAUSE_TRACE_REGISTRY_PREVIEW_1A.md`. 15 tests + review gate green. It proves a consent `root_set_hash` can be bound to a hand-reviewed clause set requiring all three roots; it does NOT prove the clause summaries authoritatively encode the roots, nor any live root-clause enforcement, live governance, or live mutation; no optimizer, model, network, daemon, token, wallet, federation, or identity binding; boundary all-false, authority_delta 0. |
| [MEASURED] Pure preview-only **FDE Isnād replay capsule** — a content-addressed capsule that preserves WHY a mission stopped and WHERE each claim came from — not only WHAT happened — and replays that verdict from the capsule ALONE, without the model. It composes four already-shipped pieces: (a) evidence references (hashes only), (b) an Isnād lineage per claim (`origin → first_appearance → author_or_model → transformation → evidence → counterevidence → verifier → status`), (c) an FDE failure diagnosis (the `dema-fde-dual-diagnostic.js` vocabulary, mirrored not reinvented), and (d) a routing decision ("Doxology route") DERIVED from that diagnosis, under authority-monotonicity. `buildCapsule` derives `source_lineage_hash`/`diagnosis_hash`/`route_hash`/`capsule_hash` and the `route` (a caller route is ignored). The routing is a pure map: `boundary_violation` → `stop` (HIGHEST precedence — wins over any other class); no lineage evidence step → `insufficient_evidence_stop`; `implementation_defect`/`test_drift` → `patch_proposal`; `proof_gap`/`doc_drift` → `proof_repair_proposal`; `environment_gap`/`dependency_gap`/`permission_gap` (outward) → `operator_or_environment_repair` (NEVER a code patch); `unknown` → `insufficient_evidence_stop`. `replayCapsule(capsule)` re-derives the route + verdict from the capsule body ALONE (no model, `model_used:false`) and sets `replay_exact = (re-derived == stored)`. `verifyCapsule({capsule})` returns verdict `BLOCK` on: a changed source `ref_hash` (`source_lineage_hash_mismatch`), missing evidence (`missing_evidence` — an `implementation_defect` with no evidence step routes to `insufficient_evidence_stop`, never `patch_proposal`), a forged route (`forged_route`, even with recomputed sub-hashes), any body field mutated after the sealed `capsule_hash` (`capsule_hash_mismatch`), an `authority_delta != 0`, and any `execution_allowed`/`mint_allowed` set true; `buildCapsule` THROWS on an input `authority_delta > 0` or `execution_allowed`/`mint_allowed:true`. Only hashes + enum labels are carried — no raw evidence text, no private key — and smuggled input fields are dropped by the field whitelist. Core law: the routing is a pure function of the diagnosis + evidence, and the verdict replays from the capsule alone — the mission survives the model. (DEMA-FDE-ISNAD-REPLAY-CAPSULE-PREVIEW-0A) | `packages/core/src/fde-isnad-replay-capsule-preview.js` + `tests/fde-isnad-replay-capsule-preview.test.js` + `scripts/review/fde-isnad-replay-capsule-preview-check.mjs` + `docs/02-architecture/DEMA_FDE_ISNAD_REPLAY_CAPSULE_PREVIEW_v0_1.md` + `docs/receipts/DEMA_FDE_ISNAD_REPLAY_CAPSULE_PREVIEW_0A.md`. 20 tests + review gate green. It proves why-a-mission-stopped plus each claim's lineage can be preserved in a model-free, tamper-evident, authority-monotone capsule and replayed from the capsule alone; it does NOT prove a live FDE runtime, a live mission, live governance, verified impact, autopatch, or live remediation — the capsule is a preview record, not an executor; the capsule is content-addressed, not Ed25519-signed; no optimizer, model, network, daemon, token, wallet, federation, or identity binding; boundary all-false, authority_delta 0, execution_allowed and mint_allowed false. |
| [MEASURED] **Persistent mission corridor control plane** — "the mission remembers itself." An immutable content-addressed contract and append-only hash-chained journal live under `$DEMA_HOME/missions/<id>/`; the closed 11-state map, lease/repair status, and resume point are derived from persisted bytes with injected time. Every write uses two-step root-bound consent; a phrase alone is never authority; the atomic nonce reservation remains burned after later failure. The 25-test suite covers contract/journal invariants, fresh-process resume, normalized roots, cross-process nonce races, and semantic replay validation: C3 closure bindings, when present, must be all-or-none, exact-shape, and legal only on `COMPLETE`; rehashed malformed bindings fail closed, while legacy `COMPLETE` events without C3 fields remain readable. (DEMA-MISSION-CORRIDOR-0A) | `packages/mission/src/mission-corridor.js` + `tests/mission-corridor.test.js` (25) + `scripts/review/mission-corridor-check.mjs` + `docs/06-adr/ADR-MISSION-CORRIDOR-0A.md` + `docs/receipts/DEMA_MISSION_CORRIDOR_0A.md`. Outside the separately bound C3 fixture route this is a control plane only: no worker, daemon, model, network, hidden execution, or process-enforced lease; `authority_delta: 0`. |
| [MEASURED] [LOCAL_ONLY] **Mission corridor closure — C3 route implemented and live-wired on current main; durable rollback proof still missing (`dema mission corridor advance` / `complete`)** — **Verified source facts.** The C3 cutover is committed at `719902e1915c994e800ee224e847ff5632987082`, merged at `18328e879016ffda5c1b858e65791adde7ef099a` (that merge lists the implementation commit as a direct parent), and both are ancestors of current main `53e636c81e2677756bc3b6b3178cb651c17ceb02`. Re-derived from committed blobs: `apps/cli/src/commands/mission.js` invokes `claimConsentNonce(...)` at line 1654; it imports `corridor-closure-gatherer.js`, the sole production caller of `openClosureTransaction` and `appendClosureEvent`; and it calls `buildClaimBoundConsentRegistry`, `appendClosureTransactionPhase` and `runTransactionalMechanicalClosure` from production, non-test code. The route is therefore not merely source-present or test-only. It binds one exact canonical C1 claim to C2 before mutation and walks `PREPARED → EFFECT_INTENT_PERSISTED → EFFECT_APPLIED → VERIFIED → SEALED → LEDGER_COMMITTED → ANCHORED → RESOLVED`; only `ANCHORED` may resolve `COMPLETED_VERIFIED`. The bounded regular-file move uses dir-fd-bound root identity and atomic no-replace target publication via `link(2)` with no `rename` fallback; a real child `SIGKILL` between target link and source unlink cold-recovers the measured two-link intermediate without a duplicate initial apply. A shared local closure-tail lock serializes ledger/anchor publication. Journal replay validates closure bindings semantically; ledger and anchor writes fsync file plus parent directory; anchor records must advance an intact ledger prefix. **Verified existing rollback capability — the effect is reversible and its restoration is computable.** `EFFECT_INTENT_PERSISTED` durably carries the prepared intent; that intent contains `before_manifest` and `before_hash`; the rename inverse is deterministically derivable by swapping the persisted `from`/`to` operands; the effect adapter implements `undo`; Omega0 can recompute the restored manifest hash and compare it against `before_hash`; and Omega0 currently computes `restoration_verified`, `restored_hash` and `undo_success_pct`. Because publication is `link`+`unlink` of one inode, a completed inverse restores the identical object rather than a reconstruction. **Measured test facts.** Recorded at `53e636c` on the sandbox harness (Linux, Node `v22.22.2`, clean extraction carrying its own git repository and a writable `HOME`): suite `8538` tests · `8535` pass · `0` fail · `3` declared skips; `npm run check` exit `0`; `npm run llm:guidance` `0` fail; claim-corpus gate `133/133`, `new=0`. Those figures bind to that recorded exact-SHA run on that harness and are not a universal property of the software. **Open C4 defect — the rollback proof is computed but never becomes durable history.** No production path appends `ROLLBACK_STARTED`; no production path appends `ROLLED_BACK`; no production path appends `RECOVERY_REQUIRED`; `BEFORE_STATE_VERIFIED` does not exist as a transaction phase; the computed restoration proof (`restoration_verified`, `restored_hash`, `undo_success_pct`) is held in memory and is not persisted into the transaction record; the transition table admits `ROLLED_BACK → RESOLVED` directly, so a rollback may settle without a recorded restoration proof; verified rollback is not durably bound to a Mission Corridor `CHECKPOINT` outcome; and failed or unverifiable rollback is not durably bound to `STOPPED` plus `RECOVERY_REQUIRED`. Failure-atomic closure therefore remains incomplete. **Limits:** Linux `/proc/self/fd` is required and unsupported platforms fail closed; process-loss at the tested two-link boundary is covered, not power-loss or every boundary; locks are local, not distributed; the first fresh-home closure has no prior ledger head; fixture authorship is not operator identity. This row does not claim real operator activation, real `DEMA_HOME` execution, independent production verification, restart-safe failure atomicity, federation, Node1 readiness, or Node0 closure; no useful-human-impact, second-host, runtime, daemon, model, network, mint, token economy or L1 activation is proved. `NODE0_CLOSED = false`. (MISSION-CORRIDOR-CLOSURE-1A + C3-CLOSURE-TX-CUTOVER-1A; C4 open) | `packages/receipts/src/consent-nonce-claim.js` + `packages/receipts/src/mission-closure-transaction.js` + `packages/core/src/omega0-mechanical-closure.js` + `packages/mission/src/mission-corridor-closure.js` + `packages/mission/src/corridor-closure-gatherer.js` + `apps/cli/src/commands/mission.js`; `tests/consent-nonce-claim.test.js` (14), `tests/mission-closure-transaction.test.js` (32), `tests/omega0-mechanical-closure.test.js` (16), `tests/mission-corridor.test.js` (25), `tests/mission-corridor-closure.test.js` (16), `tests/corridor-closure-tx-cutover.test.js` (9), `tests/mission-corridor-closure-binding.test.js` (18), `tests/canonical-ledger.test.js` (11), plus nonce atomicity/registry regressions. Ancestry re-derived with `git merge-base --is-ancestor`; call graph and phase-writer inventory re-derived from `git show <sha>:<path>` blobs across `apps/` and `packages/`. `authority_delta: 0`. |
| [MEASURED] **Deterministic Program Graph + Niche Mission Cell (representation only)** — "the graph owns topology; the journal owns history; FATE owns authority." An immutable, caller-supplied ProgramDefinition compiled into a `bizra.canonical-json.v1` hash envelope (`{canonicalization, body, body_hash}` — the hash subject is `body` alone, never its own hash) with a PURE derived ProgramProjection (initial task states + blocked reasons + program state — never caller input; lifecycle/derived fields in input are rejected by name). Closed 9-state transition table (unlisted transitions fail closed; BLOCKED/REJECTED have no exits in 0A — unblocking is Mission Corridor journal law, not graph law); structural transition candidates ALWAYS return `transition_applied: false` and `authority_granted: false` with an enumerated `does_not_prove` list — evidence refs prove presence, not truth; identifier separation is not organizational verifier independence. Forge-and-rehash fails closed: a recomputed hash over an invariant-violating, denormalized, or schema-relabeled body still fails verification, and a forged projection fails against recomputation. Profile-owned niche rules (`bizra.dema.niche-profile.founder-recovery.v0.1`: ≥3 distinct source observations) stay out of generic lifecycle law; `program_id` is validated by format only — global uniqueness belongs to a future registry. Canonical fixture DEMA-CONTINUUM-FOUNDER-RECOVERY-001 (8 linear tasks, nothing ACCEPTED, program PROPOSED) is a REPRESENTATION: no archive read, no model, no effect, no value claim. (DEMA-PROGRAM-GRAPH-NICHE-CELL-0A) | `packages/mission/src/dema-program-graph.js` + `tests/dema-program-graph.test.js` (41 tests) + `scripts/review/dema-program-graph-check.mjs` (in `npm run check`) + `docs/02-architecture/DEMA_PROGRAM_GRAPH_NICHE_CELL_v0_1.md` + `docs/receipts/DEMA_PROGRAM_GRAPH_NICHE_CELL_0A.md`. REPRESENTATION ONLY: no persistence, no consent surface, no evidence truth, no worker/model execution, no opportunity detection, no autonomous Foundry; second registered canon consumer (M5.1B allowlist); NOT in the capability truth registry (mission-domain precedent — registry row on promotion). Kernel pure (no fs/clock/random/env); boundary all-false; `authority_delta: 0`. |
| [MEASURED] **Canonical JSON v1 contract** (`bizra.canonical-json.v1`) — one versioned byte contract for FUTURE hash-bearing objects: same value → same UTF-8 bytes → same `sha256:` hash across independent implementations. Closed accepted domain (null/bool/finite numbers/well-formed strings/dense plain arrays/plain objects), closed 18-code fail-closed error registry (undefined, NaN, ±Inf, unsafe integers incl. `1e300`, lone surrogates, sparse arrays, accessors — never executed, symbol keys, non-plain objects, cycles, limits). Keys sort by Unicode code point (≠ UTF-16 order, pinned). Evidence-based limits measured over all 47 tracked JSON artifacts at ≤18.4% utilization (depth 64, total 1 MiB, keys 256, array 1024, string 64 KiB). Cross-language: JS + independent Python-stdlib verifier converge byte-for-byte on all 24 valid vectors (ECMAScript number layout re-implemented over `repr()` digits); 34 invalid vectors pin exact error codes; 20 js_only negatives explicitly skipped by Python. Motivated by the M5.0 inventory: 86 `stableStringify` definitions in 4 divergent behavioral groups already hash the same logical object differently. (CANONICAL-JSON-V1-0A / M5.1A) | `packages/canon/src/canonical-json-v1.js` + `canonical-json-errors.js` + `sha256-canonical-json-v1.js` + `packages/canon/vectors/canonical-json-v1-{valid,invalid}.json` + `tests/canonical-json-v1.test.js` (14 tests) + `scripts/review/canonical-json-v1-check.mjs` (in `npm run check`) + `scripts/review/canonical-json-v1-verify.py` + `docs/06-adr/ADR-CANONICAL-JSON-V1.md` + `docs/receipts/CANONICAL_JSON_V1_CONTRACT_0A.md`. PREVIEW_ONLY: adoption is gate-enforced by an explicit reviewed allowlist — two registered consumers (`packages/mission/src/mission-corridor.js`, introduced by the reconciled Mission Corridor candidate — promotion-event identifiers in its receipt; `packages/mission/src/dema-program-graph.js`, registered in the PROGRAM-GRAPH-NICHE-CELL-0A slice); any UNregistered importer outside tests + gate still fails the scan; no existing serializer consumer, receipt ID, or historical hash changed; no legacy adapter yet (M5.2); no scaffold change (M5.1B); Rust convergence deferred (M5.1C) — NOT full cross-language verification; no RFC 8785 compliance claimed; NOT in the capability truth registry (registered adoption alone ≠ runtime, authority, or mission capability); no historical receipt or hash migrated; no runtime, network, model, mint, PoI, federation. Boundary all-false; `authority_delta: 0`. |
| [MEASURED] Pure **Node0 agent-fleet role-contract kernel** — role-contract kernel + 12 contracts merged (7 PAT gemma-family, 5 SAT deepseek-family, `dema-alpha` outside the fleet); validation MEASURED by tests. `validateAgentRoleContract` fail-closed checks schema, `role_id` team-prefix match, team/`serves` match, `base_class` shape, `spawn_limit` within the team ceiling, and the canonical 4-key all-false authority block; `validateAgentFleet` requires exactly 7 PAT + 5 SAT, no duplicate `role_id`, and no `base_class.family` shared across teams. Agents themselves are DESIGNED_NOT_LIVE: no serving, no training, no deliberation. (NODE0-AGENT-FLEET-ROLES-1A) | `packages/core/src/agent-role-contract.js` + `packages/core/src/node0-agent-fleet-roles.js` + `tests/agent-role-contract.test.js` (19 tests) + `tests/node0-agent-fleet-roles.test.js` (4 tests) + `docs/superpowers/specs/2026-07-13-node0-agent-fleet-model-architecture-design.md`. No IO, no clock, no model invocation, no spawn, no execution, daemon, network, token, or wallet; boundary all-false. |
| [MEASURED] Pure **Node0 realm-state replay kernel** — deterministically reduces an injected hash-chained event history into derived realm state (missions, assets, descriptive scope state, head); fail-closed named blocks for chain breaks, non-integer seq, non-canonical event content (`event_not_canonicalizable`), unknown kinds, verdict-less promotion, scope widening/duplicates; null-prototype identity maps (`constructor`/`toString`/`__proto__` are ordinary keys); deep-frozen events/state/payload; verifier rejects forged-and-rehashed payloads on every declared semantic invariant. Durable storage NOT implemented; restart recovery NOT proved; independent authenticity NOT proved (external anchor = later slice). (NODE0-REALM-STATE-KERNEL-1A) | `packages/core/src/node0-realm-state-kernel.js` + `tests/node0-realm-state-kernel.test.js` (25 tests) + `scripts/review/node0-realm-state-kernel-check.mjs` (in `npm run check`) + `docs/02-architecture/NODE0_REALM_STATE_KERNEL_v0_1.md` + `docs/receipts/NODE0_REALM_STATE_KERNEL_1A.md`. No execution, daemon, network, token, wallet, or federation; boundary all-false. |
| [MEASURED] Pure **Node0 metrics-baseline kernel** — derives baseline metrics from an injected hash-chained realm event history through the realm reducer; every MEASURED metric binds the exact event seqs it derives from; absent evidence yields UNKNOWN with a named reason (never zero); corrupt history yields no metrics (reducer's named block propagates); utilization rate counts attempts in its denominator; duration metrics honestly UNKNOWN (clock-free events). (NODE0-METRICS-BASELINE-1A) | `packages/core/src/node0-metrics-baseline.js` + `tests/node0-metrics-baseline.test.js` (16 tests) + `scripts/review/node0-metrics-baseline-check.mjs` (in `npm run check`) + `docs/02-architecture/NODE0_METRICS_BASELINE_v0_1.md` + `docs/receipts/NODE0_METRICS_BASELINE_1A.md`. No execution, daemon, network, token, wallet, or federation; boundary all-false. |
| [MEASURED] Pure **DEMA recovery-mission engine kernel** — deterministic, fail-closed human-gated Recovery Mission state machine over an INJECTED event history: declare -> reconstruct -> candidates -> human revival -> use -> verify -> seal; `MISSION_DECLARED` first-event-only, `RECONSTRUCTED` requires a non-empty `consent_id` and rejects orphan candidates / more than 7, `AWAITING_HUMAN` only exits via `HUMAN_REVIVAL` naming a surfaced candidate (no auto-selection path in code), `WORKER_RESULT` is evidence only (can only reach `VERIFYING`, never `SEALED`, by itself), `VERIFIER_VERDICT` seals only on `PASS` from a verifier independent of the worker attesting the human-chosen asset (`verifier_is_generator` / `asset_not_used_in_mission` rejected), `STOP`/terminal states fail-closed; the standalone `reconstructRecoveryCandidates` helper never emits content outside the declared `source_boundary`, buckets unknown-time evidence under a literal `"UNKNOWN"` sentinel (never interpolated), carries declared contradictions verbatim, and ranks candidates by a labeled integer `rank` (never a decimal score) capped at 7. Durable event-log storage NOT implemented; restart recovery NOT proved; independent authenticity NOT proved (external anchor = later slice). (DEMA-RECOVERY-MISSION-ENGINE-1A) | `packages/core/src/dema-recovery-mission-engine.js` + `tests/dema-recovery-mission-engine.test.js` (34 tests) + `scripts/review/dema-recovery-mission-engine-check.mjs` (in `npm run check`) + `docs/02-architecture/DEMA_RECOVERY_MISSION_ENGINE_v0_1.md` + `docs/receipts/DEMA_RECOVERY_MISSION_ENGINE_1A.md`. No execution, daemon, network, token, wallet, or federation; boundary all-false. |
| [PREVIEW_ONLY] **DEMA mission worker-handoff kernel** — the minimum provable model-substitution case: a declared mission changes worker/model and records the swap as ONE deterministic hash-chained `MISSION_CHECKPOINT` (checkpoint_type `WORKER_HANDOFF`) in the existing `NODE0-REALM-STATE-KERNEL-1A` event log — no parallel state machine, no model router, no persistence, no new authority. Fail-closed unless the mission is already declared, the worker actually changes, every before/after invariant hash (mission_contract, acceptance_criteria, consent_scope, source_checkpoint) is equal, consent is an exact byte match, and `authority_delta` is 0; the verifier replays the whole returned history and rejects a forged-and-rehashed authority increase, a false continuity proof, extra envelope/event/payload fields, non-normalized set ordering, or a non-canonical all-false boundary. Proves mission continuity SURVIVES a model/worker swap with the mission contract, acceptance, consent scope, source checkpoint, and authority invariant — the state lives in the realm chain, not the model. Durable persistence, restart recovery, automatic failover, worker selection, model invocation, and independent authenticity NOT implemented; the reducer records the checkpoint seq but does not yet project `current_worker`. (DEMA-MISSION-WORKER-HANDOFF-0A) | `packages/core/src/dema-mission-worker-handoff.js` + `tests/dema-mission-worker-handoff.test.js` (28 tests) + `scripts/review/dema-mission-worker-handoff-check.mjs` (in `npm run check`) + `docs/02-architecture/DEMA_MISSION_WORKER_HANDOFF_v0_1.md` + `docs/receipts/DEMA_MISSION_WORKER_HANDOFF_0A.md`. No execution, daemon, network, token, wallet, model-invocation, or federation; boundary all-false; authority_delta 0. |
| [MEASURED] Read-only **DEMA recovery-mission gatherer + `dema recovery preview` CLI** — turns the recovery-mission engine kernel into a runnable PREVIEW by feeding real, boundary-enforced file METADATA (never content) into the reused `reconstructRecoveryCandidates` helper. A row is EXCLUDED into `not_accessed_report` (reason `out_of_source_boundary`, never a candidate) when its declared `root` is undeclared, its resolved `root + relative_path` escapes that root (path-traversal guard, pure `node:path` string arithmetic, no fs in the kernel), or it falls under a declared exclusion; a `null`/missing `mtime_iso` buckets under the literal `"UNKNOWN"` chronology sentinel; a row claiming `content_read: true` fails the WHOLE request closed at plan time (`content_read_claimed`), never silently dropped; `max_files` fails closed on overrun. Content-addressed (`bizra.canonical-json.v1`) and body-bound verified with a 9-key all-false boundary (exact declared key set, never vacuous). The ONLY fs surface is a separate read-only effect adapter (`apps/cli/src/commands/dema-recovery-mission-fs-gatherer.js`) that walks one bounded root, never follows a symlink escaping the root, and fails closed on a `max_files` cap overrun; the CLI adapter (`packages/core/src`) has no direct fs import — the gatherer is always caller-injected. (DEMA-RECOVERY-MISSION-GATHERER-1B) | `packages/core/src/dema-recovery-mission-gatherer.js` + `packages/core/src/dema-recovery-mission-cli-adapter.js` + `apps/cli/src/commands/dema-recovery-mission-fs-gatherer.js` + `apps/cli/src/commands/recovery.js` + `tests/dema-recovery-mission-gatherer.test.js` (28 tests) + `scripts/review/dema-recovery-mission-gatherer-check.mjs` (in `npm run check`) + `docs/02-architecture/DEMA_RECOVERY_MISSION_GATHERER_v0_1.md` + `docs/receipts/DEMA_RECOVERY_MISSION_GATHERER_1B.md`. NO content read; no relevance signal derived from metadata (candidates tie-break alphabetically by `asset_id`); NO auto-selection — human revival is a separate governed step (the engine's `HUMAN_REVIVAL` event), not in this slice; independent authenticity NOT proved (same declared limit as the engine kernel); no execution, daemon, network, token, wallet, or federation; boundary all-false. |
| [MEASURED] Pure kernel proving a mission-task verdict is invariant to which model produced the output: the system's acceptance contract decides ACCEPT/REJECT as a function of (output, contract) only; model identity never launders a failing output into acceptance nor changes a passing one. Three constructively-attested invariants (verdict_is_model_blind · no_identity_laundering · relabel_invariant), content-addressed + body-bound verify that fails closed on a forged invariant flag or extra boundary key. Hardened by PROOF-CONTRACT-HOTFIX-1A after three false-PASS paths were reproduced on `c64fedb`: `verify` now RE-DERIVES the claim from the payload's own candidate rows (`evidence_ok`) instead of trusting the asserted `invariants` booleans — a hash binds bytes, not truth, so a fabricated body could previously be rehashed into a PASS; both `plan` and `verify` refuse an attestation carrying no actual swap (a single candidate, or duplicate `model_id`s, made every invariant hold vacuously); and `evaluateAgainstContract` refuses a malformed, mistyped or unknown contract field rather than skipping its check, which had quietly widened acceptance to accept-everything. Extended by EFFECTIVE-CONTRACT-GUARD-1B: a well-TYPED contract could still impose zero predicates (`{}`, `required_output_keys: []`, `expected: {}`) and therefore accept every output, making the measured invariance vacuously true — `run()` returned `ok:true` with `all_hold:true` over two unrelated junk outputs. Both `plan` and `evaluateAgainstContract` now require at least one effective predicate; note `verify` cannot catch this from a MINIMAL envelope, which carries only `contract_hash` and never the predicates themselves — lifted for opt-in envelopes by TRANSPORT-REPRODUCTION-2A, below. **Two distinct guarantees, not one — do not read the stronger for the weaker.** BUILD-time (`run()` over real injected candidates) genuinely executes the contract and constructively recomputes the invariants: that is verdict-invariance, measured. VERIFY-time over a *transported* attestation establishes only ROW-DERIVED ATTESTATION CONSISTENCY — `evidence_ok` settles what the rows support (one output hash may never carry two verdicts; summary counts must be the ones the rows produce; at least two distinct models are represented) and NOT independent contract-verdict verification, since raw outputs are absent from the payload. A receiver of such a MINIMAL envelope therefore cannot confirm that each output truly satisfied the contract, that `failed_requirements` reflects a real evaluation, or that the builder honestly ran `evaluateAgainstContract`. Closed by TRANSPORT-REPRODUCTION-2A for envelopes that OPT IN: `input.transport.carry_contract` puts the validated predicates in the body, `carry_outputs` puts each judged output in its row, and `verify` returns `established` — the tier it actually derived from what the body carried, never a tier the body asserts. `rows_consistent` (nothing carried; the 1A/1B/1C guarantee, still the default) < `contract_reproduced` (predicates present, hash-bound to `contract_hash`, admissible and non-vacuous — so a receiver DOES now catch the 1B vacuity described above) < `verdict_reproduced` (outputs present, each hashing to the row it travels in, and `evaluateAgainstContract` RE-RUN per row reproducing both `verdict` and `failed_requirements`). PRESENCE IS AN OBLIGATION: anything the envelope carries is re-run, and carried evidence that fails to reproduce sets `ok:false` — it never silently degrades to a weaker tier. What 2A does NOT prove at any tier: that the carried outputs actually came from the stated `model_id`s. Model provenance remains unattested, the boundary stays all-false, and no model is invoked. The alternative closure — output commitments bound to an independently trusted evaluator receipt — is still not built. Extended again by CONTRACT-ADMISSION-GUARD-1C: counting predicates (1B) does not check SHAPE, so a contract with one good predicate beside a typo`d or mistyped field was still admitted as a proof subject — every candidate was then rejected for the malformation, and that uniform rejection satisfies all three invariants trivially, yielding a PASS-shaped attestation over a broken contract. Note this shape was a CONSEQUENCE of the 1A fix: before it a malformed contract accepted everything (loudly wrong); after it, it rejects everything, which reads as an honest `nothing met the bar`. `validateAcceptanceContract()` is now the single definition of an admissible contract and BOTH the admission gate (`plan`) and the decision function (`evaluateAgainstContract`) consume it, so the two cannot drift apart. Hardened again by PR #423 review (two P1s found independently by Codex and Greptile): a predicate outside the canonical-JSON domain (`undefined`, function, symbol, BigInt, non-finite number, Date/Map, a cycle, or a nested `undefined`) was admitted, then failed to compare — rejecting every candidate, which satisfies all three invariants trivially and produced a PASS over a contract that never compared anything; and a transported rehashed payload carrying an ACCEPT row with `output_hash: null` verified, because that row inflated `accept_count` while contributing nothing to `accepted_output_hashes` so BOTH summaries still agreed. Contracts are now validated against the canonical-JSON domain recursively and must hash as a whole, and every ACCEPT row must carry a well-formed `sha256:<64 hex>` output hash — one the builder can always produce, since an output that fails to canonicalise is rejected before it can reach ACCEPT. Hardened once more by a second PR #423 review pass (Greptile): every look at a caller-supplied contract — `Object.keys(c)`, `"expected" in c`, `c.expected`, `Object.entries(c.expected)` — runs attacker-controlled code when the contract carries an accessor or is a Proxy, and those exceptions escaped the public API, so a hostile contract crashed `validate`/`plan`/`evaluate`/`run` instead of being refused. Worse, containment alone was insufficient: the contract was re-read by the validator, again by `evaluateAgainstContract` for every candidate, and again by every invariant, so a STATEFUL accessor could present an admissible contract to the admission gate and a different one to the builder, and the attestation would bind neither. The contract is now read exactly ONCE, inside a single containment boundary, into an inert frozen snapshot; a read that throws is refused as `contract_uninspectable`, and every downstream consumer (classification, the three invariants, `contract_hash`, and the evaluator) uses only the snapshot — never the caller's object. Closed on the candidate side by the same audit: the contract was snapshotted but candidates were not, and `classifyCandidates` evaluated `cand.output` and then hashed it through a SECOND read while every invariant read it again — seven reads per build. A stateful candidate was therefore judged as one value and hashed as another; measured, `run()` returned `ok:true` with a content_hash whose ACCEPT row carried bytes that were never accepted, which falsifies the attestation's central claim. The complete proof input (task, acceptance contract, candidate `model_id`, candidate output) now crosses ONE guarded snapshot before planning, with the output normalised through the single canonical byte contract, so verdict and `output_hash` derive from the same inert bytes; an uninspectable candidate fails closed as `candidate_uninspectable:<index>`, and field presence is own-property, so an inherited member never satisfies a predicate while a JSON-derived own `__proto__` field legitimately does. One further false-PASS closed in the same family: `raw ?? null` coerced an own `output: undefined` — a value OUTSIDE the canonical-JSON domain — into canonical `null`, which is inside it, so evaluation and hashing certified a value the candidate never supplied; measured, a candidate carrying `output: undefined` produced an ACCEPT row bearing the canonical hash of null under a null-tolerant contract. Normalisation may preserve meaning but must never manufacture it, so the canonical serializer is now the sole arbiter (it refuses `undefined` as `value_undefined` and accepts `null`): three states stay distinct — an absent `output` blocks at plan, an own `output: undefined` is refused as `output_not_canonicalizable` with NO hash, and an explicit `output: null` remains a valid canonical value evaluated normally against the contract. The identical coercion survived one level down at field granularity — `ownField(output, k) ?? null` made an ABSENT field canonicalise to "null" and satisfy a declared `expected: { answer: null }`, so a predicate was met by a field the model never produced — and presence and value are now asked as separate questions, while an explicitly declared null field still matches. The measured form of "the LLM is a replaceable component; system state is authoritative." Judges INJECTED outputs — no live model call, no ranking of models, no claim about any model's quality. (NODE0-MODEL-SWAP-INVARIANCE-1A · NODE0-MODEL-SWAP-TRANSPORT-REPRODUCTION-2A) | `packages/core/src/node0-model-swap-invariance.js` + `tests/node0-model-swap-invariance.test.js` (62 tests) + `scripts/review/node0-model-swap-invariance-check.mjs` (in `npm run check`) + `docs/receipts/NODE0_MODEL_SWAP_INVARIANCE_1A.md`. No execution, daemon, network, token, wallet, model-invocation, or federation; boundary all-false. |
| Compose the proven reversible-rename, sanitizer, consent and receipt primitives into one bounded, consented, fully-reversible multi-file steward job (RENAME-only, metadata-only, no model/network). (DEMA-REVERSIBLE-FILE-STEWARD-1A) | `packages/core/src/dema-reversible-file-steward.js` + `tests/dema-reversible-file-steward.test.js` + `scripts/review/dema-reversible-file-steward-check.mjs` + `docs/receipts/DEMA_REVERSIBLE_FILE_STEWARD_1A.md`. [MEASURED] the pure planner: builds a bounded, exact-consent-gated, sanitizer-gated, content-addressed, fully-reversible multi-RENAME job plan with a body-bound verifier and an all-false boundary. Execution + undo (fs) delegate to `node0-reversible-execute-gate` in a later slice — NOT proven here. No execution, daemon, network, token, wallet, or federation. |
| Compose the proven reversible-rename, sanitizer, consent and receipt primitives into one bounded, consented, fully-reversible multi-file steward job (RENAME-only, metadata-only, no model/network). (DEMA-REVERSIBLE-FILE-STEWARD-1A) | `packages/core/src/dema-reversible-file-steward.js` + `tests/dema-reversible-file-steward.test.js` + `scripts/review/dema-reversible-file-steward-check.mjs` + `docs/receipts/DEMA_REVERSIBLE_FILE_STEWARD_1A.md`. [MEASURED] the pure planner: builds a bounded, exact-consent-gated, sanitizer-gated, content-addressed, fully-reversible multi-RENAME job plan with a body-bound verifier and an all-false boundary. Execution + undo (fs) are proven in DEMA-REVERSIBLE-FILE-STEWARD-1B, not in this planner. No execution, daemon, network, token, wallet, or federation. |
| Sequenced **transactional** execution + proven undo over the shipped reversible-rename gate — the whole steward job round-trips against a real fs, and a partial failure auto-rolls-back to genesis. (DEMA-REVERSIBLE-FILE-STEWARD-1B) | `packages/core/src/dema-reversible-file-steward-execution.js` + `tests/dema-reversible-file-steward-execution.test.js`. [MEASURED] sandbox-scoped reversible file mutation with proven undo: execute-all → undo-all returns the sandbox's user-file set to genesis (6 real-fs tests), each undo proven against its independent on-disk backup. **All-or-nothing:** a later-atom failure reverses every already-applied atom (net `executed_count` 0); a rollback undo that cannot prove surfaces as `execute_stopped_rollback_incomplete` — never a silently-left partial mutation. Executed boundary — sandbox_only · reversible · undo_available true; network/model/token/wallet/federation false. CLI surface in DEMA-REVERSIBLE-FILE-STEWARD-1C; no model, no network, no economic surface. |
| `dema steward plan\|verify\|run\|undo` CLI surface over the proven steward kernels. (DEMA-REVERSIBLE-FILE-STEWARD-1C) | `apps/cli/src/commands/steward.js` + `tests/steward-cli.test.js` (7 spawned-CLI tests) + consent-matrix row (`local_write` · `exact_phrase`). [MEASURED] thin CLI binding only: plan is a pure preview (exact preview phrase); run/verify mutate only inside the job's `sandbox_root` via the shipped reversible-execute gate after the exact execute phrase; undo restores from receipts with per-atom backup proof. The CLI adds no policy of its own. No model, no network, no daemon, no token, no wallet, no federation. |
| Durable local season state: save, status and resume a bounded Node0 continuation checkpoint from disk alone. (NODE0-MINIMUM-SEASON-SAVE-RESUME-1A) | `packages/core/src/node0-minimum-season-save-resume.js` + `tests/node0-minimum-season-save-resume.test.js` + `scripts/review/node0-minimum-season-save-resume-check.mjs` + `docs/02-architecture/NODE0_MINIMUM_SEASON_SAVE_RESUME_v0_1.md` + `docs/receipts/NODE0_MINIMUM_SEASON_SAVE_RESUME_1A.md`. [MEASURED] 28 focused tests green, incl. a real process-restart reconstruction (S3), real process death before/at/after HEAD publication (S6/S6b/S7) and a real two-process fence race (S13). LOCAL_ONLY single-node storage under DEMA_HOME. No semantic memory, no cross-host replication, no autonomous execution on resume, no daemon, no network, no token, no wallet, no federation. Pending consent is preserved, never inferred or granted. |
| Stdlib-only dependency posture                                                                         | 0 production deps, 0 devDeps in `package.json`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 |
| Local profile / setup loop                                                                             | `dema setup` is idempotent and writes only to `$DEMA_HOME` (default `~/.dema/`) — see `packages/installer/src/setup.js`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        |
| Exact-string consent gate                                                                              | `packages/fate/src/fate.js` — strict `===` byte match, fail-closed                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             |
| Approval gate (L0–L5 matrix)                                                                           | `packages/core/src/approval-gate.js` + 16 tests                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| Receipt store (read/list only)                                                                         | `packages/receipts/src/receipt-store.js` — symlink-aware containment, max-files + max-bytes caps; **NO mint surface**                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          |
| Onboarding Seal v0.1 (9-invariant regression contract)                                                 | `packages/core/src/onboarding-seal.js` + 23 tests                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              |
| Layer 1 artifact safety eval                                                                           | `packages/core/src/artifact-safety-eval.js` + `npm run eval:layer1` + 15 tests                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 |
| Envelope schema validator v0.1 (covers `onboarding_seal`, `artifact_safety_eval`, `proof_room_bundle`) | `packages/core/src/envelope-schema-validator.js` + `packages/core/schemas/*.v0.1.json` + 20 tests                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              |
| Layer 1 pipeline schema validation (wired)                                                             | `artifact-safety-eval.scanSchema()` delegates to `validateAgainstRegistry`; structural violations of a known envelope schema produce `SCHEMA_VIOLATION` verdict and exit 1 from `npm run eval:layer1`. Tests: `tests/artifact-safety-eval-schema-wiring.test.js` (7 scenarios)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 |
| Local model evaluation baseline (MODEL-EVAL-BASELINE-1A)                                               | `dema models discover` / `dema eval baseline --suite bizra-local-small` / `dema eval compare` + `packages/core/src/model-eval-baseline.js`. Pure-kernel deterministic scoring over a frozen 6-task LOCAL suite (reachability · latency · JSON obedience · code micro-task · overclaim-resistance · truth-boundary); content-addressed; gatherer is localhost-only with injected fetch. Does NOT prove model correctness, MoE, KV-cache, RSI, federation, council, economy, or any leaderboard ranking. Tests: `tests/model-eval-baseline.test.js` (8) · `tests/model-eval-baseline-cli.test.js` (4). |
| Layer 2 rubric pack + verdict validator (paste-back)                                                   | `packages/core/src/eval-layer2-rubrics.js` + `packages/core/src/eval-layer2-verdict-validator.js` + 2 schemas + `dema eval layer2 prompts` / `dema eval layer2 verify <abs-path>` CLI surfaces. **Data-only** — Dema does NOT invoke any LLM. Operator runs the judge externally and pastes verdicts back; Dema validates them structurally + semantically. ADR-016 records the design. Tests: `tests/eval-layer2-rubrics.test.js` (10) · `tests/eval-layer2-verdict-validator.test.js` (15) · `tests/eval-layer2-schema-registry-wiring.test.js` (3) · `tests/eval-layer2-cli.test.js` (9).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   |
| Local LLM adapter — localhost-only Ollama invocation (model-broker-local-invocation v0.1)              | `packages/core/src/llm-adapter.js` `invokeLocalLLM()` — invokes a localhost-only Ollama model at `http://localhost:11434/api/generate` under exact-string consent · **6 sequential gates**: (1) localhost-bound endpoint · (2) model in whitelist · (3) prompt within bounds (non-empty AND ≤ 100,000 chars) · (4) consent phrase exact-string match (ADR-005) · (5) consent freshness (replay-resistant per process) · (6) inbound prompt Layer 1 safety scan · bidirectional Layer 1 safety scan (inbound prompt + outbound response with `[REDACTED: <verdict>]` preview on hit) · `AbortController` 60s default timeout · canonical 16-key **runtime-emission** sibling boundary distinct from preview-boundary (6 keys may be `true` only on MEASURED truth*label; `network_used` / `prompt_executed` / `runtime_execution_performed` reflect \_fetch-attempted*, while `model_loaded` / `model_invocation_performed` reflect _completed-success_ — so partial-fetch failures still emit `network_used=true`) · `verdict_role: "suggestion"` enforced by schema · monotonic per-process replay-resistant consent. **v0.1 scope: one invocation per process**. `consentPhraseFor(model)` is deterministic, so once a (model, phrase) pair is consumed in a process, it cannot be replayed in the SAME process. Each `dema model-broker invoke` CLI run is a fresh Node process, so the constraint is invisible to operator-driven use. Future programmatic surfaces (PAT-7 / SAT-5 live agent teams · v0.2+ via their own ADRs) that need multi-invocation per process will require either the `__resetInvocationFreshness()` test hook or a v0.2 phrase-with-attempt-N redesign. Result envelope `bizra.dema.llm_invocation_result.v0.1` (structurally validated by Layer 1 envelope-schema-validator). Malformed 200-OK responses (missing or non-string `body.response`) are explicitly rejected as `malformed_response_payload`. ADR-018 records the design. Tests: `tests/llm-adapter.test.js` (current floor; ADR-018 v0.1 surface coverage + post-review hardening) · `tests/preview-boundary.test.js` (runtime-emission-boundary sibling covered). |
| Public-safe proof-room variant                                                                         | `artifacts/proofs/proof-room-v0.1-public-safe/` (Layer 1 verdict `PUBLIC_SAFE`)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| CodeQL security scan                                                                                   | `.github/workflows/codeql.yml` (per-PR + weekly cron)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          |
| ARTIFACT-011 — first bounded-diagnostic receipt                                                        | `~/.dema/receipts/artifact-011.json` (issued 2026-05-06 by the governed gateway; admissibility verdict `Permit`; chain length 8)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               |
| Pre-push local gate (operator-side μ-layer test orchestrator)                                          | `.git/hooks/pre-push` (operator-installed)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     |
| Node0 proof-of-truth control plane (LOCAL_ONLY release ledger)                                         | `packages/core/src/node0-proof-of-truth-control-plane.js` + `scripts/audit/node0-proof-of-truth-control-plane.mjs` + `scripts/review/node0-proof-of-truth-control-plane-check.mjs` — joins formal/cryptographic/empirical/economic rails into one frozen JSON verdict (`BLOCKED` \| `READY_LOCAL` only in 1A); `receipt_hash` via `sha256(stableStringify(body))`; hermetic review gate in `npm run check`; `npm run proof:truth` / `npm run proof:truth:check`; does NOT prove remote release, public safety, live token/URP/PoI/federation, or autonomous runtime. Tests: `tests/node0-proof-of-truth-control-plane.test.js` (15). |
| Node0 proof artifact export bundle (LOCAL_ONLY replay manifest)                                        | `packages/core/src/node0-proof-artifact-bundle.js` + `scripts/proof/export-node0-proof-artifact-bundle.mjs` + `scripts/review/node0-proof-artifact-export-check.mjs` — composes ledger + release verdict + optional CI attestation into manifest/replay artifacts; stdout JSON by default; `--write` requires exact micro-consent phrase; hermetic gate in `npm run check`; `npm run proof:export` / `npm run proof:export:check`; does NOT write without consent, prove remote release, or activate governed runtime. Tests: `tests/node0-proof-artifact-bundle.test.js`. |
| Node0 space index onboarding census (DEMA-NODE0-SPACE-INDEX-1A)                                       | `packages/core/src/node0-space-index.js` + `apps/cli/src/commands/node0-index.js` + `tests/node0-space-index.test.js` + `scripts/review/node0-space-index-check.mjs` — local-only metadata census; optional content hashing requires exact root-bound consent; checkpoints write only under `DEMA_HOME/node0-index/checkpoints`. It does not delete, move, reorganize, upload, summarize content, invoke SAT, mint, connect nodes, or mutate the scanned root. |

## Framework kernels (V2 set — deterministic validators, shipped #233–#238)

These five kernels are sometimes referred to collectively as a "reasoning
framework." That label oversells them. On disk each is a **deterministic
validator** — it performs no learned inference, no sampling, no text
generation, and no runtime execution. The `reasoner` / `HHMM` / `diffusion`
names describe the *shape* of a lifecycle/denoising metaphor, not a live ML
algorithm. Each kernel's own source header already states this; this table
lifts that disclaimer to the honesty map so the naming cannot mislead a reader
who only sees the README.

| Surface (truth label: MEASURED) | What it actually computes | Evidence |
| ------------------------------- | ------------------------- | -------- |
| HHMM lifecycle state machine (deterministic, NOT ML) | Infers a Node0/Dema lifecycle state from observable evidence via a fixed transition table + per-emission confidence constants. Header: "NOT machine learning, NOT learned probabilistic inference." | `packages/core/src/hhmm-state-machine.js` (lines 4–6, 51); `tests/hhmm-state-machine.test.js` |
| Hash-table knowledge index (6-axis, deterministic) | Indexes entries across 6 fixed axes (`HASH_TABLE_AXES`), requires an evidence anchor per entry, content-addressed `bucket_hash`. | `packages/core/src/hash-table-knowledge-index.js` (lines 5, 61–70); `tests/hash-table-knowledge-index.test.js` |
| Self-awareness report (blind-spot ledger, NOT consciousness) | Classifies each declared capability `EVIDENCED` / `BLIND_SPOT` / `NOT_KNOWN` by presence of an evidence anchor (`evidence.length > 0`). `claims_consciousness: false`; no introspection, model, or runtime. | `packages/core/src/self-awareness-report.js` (lines 6–12, 77–79); `tests/self-awareness-report.test.js` |
| Self-loop OODA cycle (bounded review, ACT cannot execute) | Orders + hash-binds 5 OODA phases (observe→orient→decide→act→review). `act_phase_must_not_execute`; `executed` forced false. NOT an autonomous loop, daemon, or scheduler. | `packages/core/src/self-loop-ooda.js` (lines 4, 92–101); `tests/self-loop-ooda.test.js` |
| Diffusion-reasoner (deterministic denoising-convergence, NOT a neural model) | `scoreDraftNoise` counts speculation/overclaim markers from a frozen lexicon; "convergence" = noise non-increasing toward an evidence-bound claim. Header: "NOT a neural diffusion model, NOT learned sampling, NOT stochastic, NOT generative." CLI: `dema diffusion refine` / `verify`. | `packages/core/src/diffusion-reasoner.js` (lines 18, 49, 73–84); `tests/diffusion-reasoner.test.js` + `tests/diffusion-cli.test.js` |

## What is DESIGNED_NOT_LIVE

| Surface                                                 | Spec                                                                                                       | What is missing for MEASURED                                                                                                                                                                                                                           |
| ------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| Node1 / federation between nodes                        | `docs/02-architecture/dema-a2a-message-envelope-v0.1.md`                                                   | Real second-node ceremony beyond schema preview; no live federation handshake                                                                                                                                                                          |
| URP (Universal Resource Pool) shared runtime            | `docs/HOUSE_OF_WISDOM_UKE_URP_CANON_v0_1.md`, `packages/core/src/urp-local.js`                             | URP is local-only; the shared/economic lane is schema + previews, not runtime                                                                                                                                                                          |
| **DESIGNED_NOT_LIVE** Proof-of-Impact / token economy   | `docs/02-architecture/dema-urp-resource-offer-v0.1.md`, `docs/02-architecture/DUAL_TOKEN_POI_ECONOMY_v0_1.md` | BZR-C/BZR-I are preview-only designed tokens; local PoI mint preview exists, but there is no wallet, no sale, no live token mint, no live PoI runtime, and no economic settlement                                                                       |
| **DESIGNED_NOT_LIVE** Absence Steward Preview            | `docs/02-architecture/ABSENCE_STEWARD_PREVIEW_v0_1.md`                                                        | Design for future bounded absence stewardship consuming receipted Away Contracts (AWAY_CONTRACT_1A). States/transitions/exit-criteria only — no runtime, no daemon, no scheduler, no `dema away start` (does not exist), no model invocation, no unattended execution |
| **DESIGNED_NOT_LIVE** Absence Steward Return Review      | `docs/02-architecture/ABSENCE_STEWARD_RETURN_REVIEW_v0_1.md`                                                  | Design for the future post-absence review ceremony: report shape, all-false boundary object, verdicts (WORK_COMPLETE deliberately undefined), refusal language (`NO_RECEIPT — cannot claim.` / `NOT_LIVE — no steward runtime exists.`). No kernel, no CLI, no receipt, no queue — and no queue may be designed before this review path is real (condition met 2026-07-04: readiness #21 + return review #22 both measured) |
| **PARTIAL: proposal-spine measured, queue DESIGNED_NOT_LIVE** Absence Steward Local Queue | `packages/core/src/absence-steward-queue-{schema,verify,receipt}.js` + `apps/cli/src/commands/away.js` (`dema away queue draft`) + `scripts/review/absence-steward-queue-check.mjs` + `tests/absence-steward-queue-{schema,verify,receipt}.test.js` + `tests/away-queue-cli-draft.test.js` + `docs/02-architecture/ABSENCE_STEWARD_LOCAL_QUEUE_v0_1.md` | The **proposal spine** is IMPLEMENTED and tested: a fail-closed item-shape validator, a body-bound launder-detecting verifier, a consent-gated receipt writer, and `dema away queue draft` (validate-only). Item states capped at PROPOSED/HUMAN_APPROVED/HUMAN_REJECTED/WITHDRAWN/EXPIRED_WITH_CONTRACT — no EXECUTING, no DONE, no execution states in vocabulary; queue membership is never consent; AUTO_DEQUEUE/SELF_APPROVAL/EXECUTION_FROM_QUEUE reject. What remains DESIGNED_NOT_LIVE: the **queue itself** — no runner, no scheduler, no daemon, no approval, no execution, no runtime, no model invocation, no network, no wallet, no mint, and no `dema away start` (does not exist). Registered as registry capability `ABSENCE_STEWARD_QUEUE_PROPOSAL_SPINE_1A` (see the MEASURED row above and `docs/receipts/ABSENCE_STEWARD_QUEUE_PROPOSAL_SPINE_1A.md`); registration covers the proposal spine only — the queue itself remains DESIGNED_NOT_LIVE. |
| **PARTIAL · [MEASURED] fixture-only** Ed25519 authorship identity, signing, and trust-bound verification | `packages/receipts/src/authorship-{signature,key-store,verify}.js`, `docs/06-adr/ADR-047-identity-pair-coherence-generation-store.md`, `docs/06-adr/ADR-048-active-signer-trust-and-history-integrity.md` | Generation-store custody, consented local signing, and public-only active-signer verification are implemented and fixture-measured. Portable history is explicitly `SIGNATURE_INTEGRITY_ONLY`. The rotate **capability** is MEASURED under AUTHORSHIP-KEY-ROTATE-1B (see MEASURED table). Still not live/complete: the founder's real key has not been rotated in ceremony; no real old-key revocation, new-key acceptance, or founder acceptance on `~/.dema/keys/` has occurred. Distinct from TASK-029 (Node0 mission-signing key). |
| Behavioral modulation runtime                           | `packages/core/src/behavioral-modulation.js`                                                               | Preview-only; no live tone-adjustment runtime                                                                                                                                                                                                          |
| Layer 2 LLM-as-judge runtime (automated judging)        | `docs/06-adr/ADR-016-eval-layer2-scaffold-only.md` (path b: scaffold-only)                                 | Dema does NOT invoke an LLM in v0.1. Operator runs the judge externally. v0.2 candidates: `--judge-cmd` operator-supplied invocation OR `dema model-broker` routing once model-broker hardens out of DESIGNED_NOT_LIVE.                                |
| MCP capability descriptor                               | `docs/02-architecture/dema-mcp-capability-descriptor-v0.1.md`                                              | Descriptor preview, no live MCP runtime in Dema                                                                                                                                                                                                        |
| QR / mobile consent companion                           | `docs/02-architecture/dema-mobile-qr-consent-v0.md`                                                        | Preview only; QR + manual echo flow on paper                                                                                                                                                                                                           |

## What is PLANNED

| Surface                                                              | Notes                                                                                                     |
| -------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------- |
| Terminal installer URL `install.bizra.ai/dema/install.sh` (+ `.ps1`) | Endpoint does not resolve yet; planned for packaged alpha release                                         |
| Installer SHA-256 hash publication per release tag                   | Will live in `docs/INSTALLER_ARCHITECTURE.md` when terminal installer goes live                           |
| macOS notarization + Windows code-signing of installer binaries      | Roadmap; see `docs/INSTALLER_ARCHITECTURE.md`                                                             |
| `dema first-run` single-command entry                                | Composes welcome → setup → status → doctor; spec exists, command pending (see `docs/FIRST_RUN_WIZARD.md`) |
| SBOM emission at release                                             | Stdlib-only surface; SBOM script not yet present                                                          |
| Dependabot for GitHub Actions SHA-pinned bumps                       | `.github/dependabot.yml` not yet present                                                                  |
| Release workflow                                                     | Manual today; `release.yml` workflow not yet present                                                      |
| Coverage headroom above the hard 95/84/95 floor                      | `npm run coverage` now enforces native Node thresholds; branch coverage remains the tightest margin and should be expanded before raising the floor |
| Expanded threat model in `SECURITY.md`                               | Current `SECURITY.md` is the non-negotiables list, not a STRIDE-style threat model                        |

## What is LOCAL_ONLY (works locally, not share-safe by default)

| Surface                                                   | Why it is not share-safe                                                                                                                                 |
| --------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `artifacts/proofs/proof-room-v0.1/proof-room-bundle.json` | Contains the operator's absolute `repo_root` path; Layer 1 eval classifies this bundle as `LEAKAGE_DETECTED`. Use the `-public-safe` variant for sharing |
| `~/.dema/receipts/*.json`                                 | Local receipts are not designed to be shared outside the operator's machine without an explicit consent + redaction pass                                 |
| `.proof-forge/`                                           | Operator-local proof receipts; gitignored by design                                                                                                      |

## Public deployment claim containment

The initial `2026-07-24` live-surface audit found that `bizra.ai` was publishing
unbound pinned metrics, “Live Receipt Chain,” “Live Network Data,” absolute
enforcement wording, live economic/URP implications, and unauthenticated API
responses carrying unbound operational claims. A later exact-SHA deployment
and credential-free crawl record the containment state separately; they do not
turn the corrected web boundary into evidence of Node0 runtime capability.

| Surface | Truth state | Current limit |
| --- | --- | --- |
| Initial live `bizra.ai` claim surface observed 2026-07-24 | `UNKNOWN` for unsupported facts; `FORBIDDEN` for live economy, URP, or federation claims | Historical claim defect. Do not cite the captured legacy copy as evidence of runtime, identity, security, scale, economics, or federation. |
| GitHub `Production – award-winner-design` deployment record | `VERIFIED` within the record's scope | GitHub deployment `5590104450`, created by `vercel[bot]`, reports `success` at exact `award-winner-design` commit `6f7f545e6a1ac044cbb8d29a0a215e8a9f2885bf`. Its environment URL is a Vercel deployment URL; this does not directly prove the `bizra.ai` alias. |
| Corrected public pages, static text, metadata, and fixed unauthenticated APIs | `MEASURED` from `2026-07-24T17:11:43.617Z` through `2026-07-24T17:11:46.037Z`; exact-source relationship `DERIVED` | Credential-free crawl covered `62` inventoried surfaces: `5` HTTP 200, `30` contained redirects, `27` expected-private 401, and zero truncation, request errors, private 200s, containment failures, or known forbidden phrases. The internally recorded completion follows the latest embedded API `measured_at` (`2026-07-24T17:11:45.595Z`). A committed raw-body-in-memory scanner ran from `2026-07-24T17:11:52.390Z` through `2026-07-24T17:11:53.245Z`, retained per-route status/byte length/SHA-256/link and match results, and found zero public receipt-link or revoked-key-link matches without retaining raw bodies. Root body SHA-256: `473052c4bbcf7bf6092bbd9c075abf70c12e6b517878fe32c5ba3521fcaf18dd`. A live same-origin runtime asset embeds Vercel deployment identifier `dpl_C7hFkz6LZRSPK1XMHAXUYwJRJj2R`, matching the exact-commit Vercel status and deployment `5590104450`; the provider alias API was not readable, so the relationship is not promoted to `VERIFIED`. This is a dated web observation, not system-health proof. |
| Authentication integrity beyond public containment | `UNKNOWN` | Known source-embedded demonstration credentials were removed and public login/refresh are held closed, but session invalidation, external identity-provider repair, secret rotation, and restored authentication are not proven. |
| Current Dema evidence update | Source presence `VERIFIED`; distribution qualifier `LOCAL_ONLY` | Post-deploy capture and deployment binding exist on the Dema review branch but are not remotely published until separately authorized push and review. |
| Public signing identity | `UNKNOWN` | Signer rotation is pending. No new signed public Claim Receipt is issued by the containment slice. |

See
[`audits/BIZRA_AI_PUBLIC_CLAIM_CONTAINMENT_1A.md`](audits/BIZRA_AI_PUBLIC_CLAIM_CONTAINMENT_1A.md)
for the route/source/evidence inventory, exact claim dispositions, candidate
paths, and post-deploy closure gates.

## Hard non-claims

These phrases are forbidden in any Dema-issued artifact unless each is
explicitly qualified on the same line as `DESIGNED_NOT_LIVE`,
`preview-only`, or `not live`. The Layer 1 artifact-safety scanner
enforces this as a regression gate:

- URP is live → status today: DESIGNED_NOT_LIVE (URP shared runtime is not live)
- Nodes are synchronized → status today: DESIGNED_NOT_LIVE (node federation is not live)
- Federated network is live → status today: DESIGNED_NOT_LIVE (preview-only)
- Public token economy is active → status today: **DESIGNED_NOT_LIVE** (no token economy is live)
- Proof-of-Impact rewards are active → status today: DESIGNED_NOT_LIVE (PoI is not live)
- Chain-bound mint is active → status today: DESIGNED_NOT_LIVE (preview-only)
- Chain-bound proof is live → status today: DESIGNED_NOT_LIVE (preview-only)
- Distributed intelligence network is live → status today: DESIGNED_NOT_LIVE (preview-only)

## How to verify this page

```bash
# Test count (locally)
node --test tests/*.test.js | tail -5

# Onboarding Seal posture on the operator's machine
node --test tests/onboarding-seal.test.js

# Proof-room public-safe variant (must pass PUBLIC_SAFE)
npm run eval:layer1 -- --artifact "$(pwd)/artifacts/proofs/proof-room-v0.1-public-safe/proof-room-bundle.json"
```

> Note: the Layer 1 artifact-safety scanner is designed for
> runtime-generated structured artifacts (proof-room bundles, mission
> receipts, evidence packets), not prose documentation. Markdown docs
> like this one legitimately reference the operator's Dema home
> directory by name for documentation purposes — running the scanner
> against prose will flag those references. Read [TESTING.md](TESTING.md)
> for the surfaces eval:layer1 is meant to gate.

## When this page changes

Anyone promoting a row from `PLANNED` or `DESIGNED_NOT_LIVE` to `MEASURED`
must add the evidence path to the "What is MEASURED" table at the same
time. Promotion without evidence is the failure mode this page exists to
prevent.

---

Public-claim containment addendum refreshed: 2026-07-24. This refresh records
the `bizra.ai` incident boundary, exact-SHA site deployment provenance, and
dated post-deploy crawl only; it does not re-audit or promote the capability
table.

Capability-table baseline last refreshed: 2026-06-28
(DEMA-CAPABILITY-TRUTH-REGISTRY-1A branch, based on merged #301 commit
`2f81517`). Refresh trigger: DEMA-CAPABILITY-TRUTH-REGISTRY-1A test-count and
surface row update.

> Scope note for the 2026-06-27 refresh: corrected test count (5,396 → 5,813)
> and rowed Node0 proof artifact export bundle (1A) as MEASURED with evidence
> paths. Prior 2026-06-24 note (2,489 → 5,396, framework kernels #233–#238)
> remains historical. Full row-by-row re-audit of every shipped surface since
> 2026-05-27 is still a separate pass; this note exists so the page does not
> overclaim its own completeness.
| Season action authority contract (NODE0-SEASON-ACTION-AUTHORITY-1A) | `packages/core/src/node0-minimum-season-save-resume.js` + `tests/node0-season-action-authority.test.js` + `tests/node0-season-action-authority-non-claims.test.js`. [MEASURED] Season State now has a machine-checkable canonical action-eligibility contract and an optional binding to one prepared C4D effect. The contract grants no consent, no FATE verdict and no execution authority. **Updated 2026-08-05:** the predicate now has one caller — the corridor Season consent preflight (row below). `FATE_EFFECT_ROUTE_INTEGRATION_ABSENT` remains **`true`**. Does NOT prove that `must_not_repeat` is enforced at an effect boundary, that the corridor effect route is Season-gated, that a local mission executed, that FATE is present, or that Node0 is operational or autonomous. |
| Closure-sprint correction (NODE0-CLOSURE-SPRINT-CORRECTION-1A) | `apps/cli/src/commands/mission.js` + `apps/cli/src/commands/node0-run.js` + `packages/mission/src/health-snapshot.js` + `tests/node0-closure-sprint-correction.test.js` (8). [MEASURED] [LOCAL_ONLY] Corrects four defects that passed a full green matrix in the first closure sprint. **D1** the runner derived sample health from `snap?.ok`, which `buildHealthSnapshot` never returns — `undefined !== false` made every sample healthy, including one attesting `mission_verdict: FAILED`; health is now derived only from `attests.mission_verdict`, and only `CLEAN` is healthy. **D2** the snapshot read `process.env.DEMA_HOME` independently of the runner's `--dema-home`, so an endurance record could be written for one installation while another was inspected; `buildHealthSnapshot` now accepts an explicit `demaHome` (additive — omitting it preserves prior behaviour) and each sample records `requested_home`, `inspected_home` and `home_matches`. **D3** the FATE check ran AFTER the single-use nonce claim and closure-lock acquisition while its refusal message claimed "nothing was written", which was false; the gate now runs before consent, nonce and locks. **D4** the effect path called only `assessReversibility`/`assessBlastRadius`, skipping Season eligibility, repository binding, policy version and effect kind; it now calls the complete typed `evaluateFatePolicy` with an independently measured executing-repository binding. **`CORRIDOR_RENAME_SEASON_GATE_LIVE` and `MUST_NOT_REPEAT_EFFECT_GATE_ENFORCED_FOR_CORRIDOR_RENAME` are now TRUE for the corridor rename route specifically** — `--season <id>` and `--dema-home <path>` are required for that effect. Global enforcement remains FALSE. Does NOT prove: tamper-evident endurance evidence (the JSONL is not yet hash-chained), a completed end-to-end reversible-effect loop with crash recovery, or any endurance target. `ONE_NODE_24H_HEALTH_PROVEN`, `ONE_NODE_72H_HEALTH_PROVEN`, `DEMA_ACTIVE_LOCAL` and `NODE0_CLOSED` all remain false. |
| FATE constitutional policy contract (NODE0-FATE-CONTRACT-1A) | `packages/core/src/node0-fate-contract.js` + `tests/node0-fate-contract.test.js` (15). [MEASURED] [LOCAL_ONLY] An **independent** constitutional policy decision — canon's Mind Three — separate from root-bound human consent. It consumes a verified Season authority verdict and a declared effect, and judges the effect's SHAPE: known kind, zero authority delta, provable reversibility (a before-manifest and before-hash, plus a declared inverse), and bounded blast radius. It takes **no phrase**, which is precisely why it can run BEFORE consent. It does not import, modify or reimplement `packages/fate/src/fate.js` (that file is the exact-phrase consent helper and remains unchanged). The independence property is asserted directly: FATE refuses an effect a human has already consented to. `authority_delta: 0`; grants no consent and no execution on any path. Now gates BOTH the corridor preflight and the real effect path (immediately before `runTransactionalMechanicalClosure`). Does NOT prove: `MUST_NOT_REPEAT_EFFECT_GATE_ENFORCED`, `CORRIDOR_RENAME_SEASON_GATE_LIVE`, that all DEMA effects are FATE-gated, or that any effect executed. |
| Node0 runner and endurance judgment (NODE0-RUN-1A + NODE0-ENDURANCE-1A) | `apps/cli/src/commands/node0-run.js` + `packages/core/src/node0-endurance.js` + `tests/node0-run.test.js` (5) + `tests/node0-endurance.test.js` (10). [MEASURED] [LOCAL_ONLY] `dema node0 run` starts the node as an **explicit foreground process** — no daemon, no fork, no detach, no model, no network, no effect — samples health on an interval, and appends each observation as an fsynced JSONL line so a crash loses at most the sample in flight. The pure endurance kernel judges the resulting record. Its governing property: **a missing sample is UNKNOWN, never PASS** — coverage is judged by GAPS, not totals, so a run that sampled for 2h, went dark for 40h and resumed reads `BROKEN`, not a 42h healthy run. Proven against a REAL `SIGKILL` and late resume (R3). Verdicts: `INSUFFICIENT` / `BROKEN` / `DEGRADED` / `HEALTHY`. `authority_delta: 0`; receipts carry boundary all-false. Does NOT prove: that any endurance target has actually been MET — a target is met only by an actual elapsed run producing a `HEALTHY` verdict; `NODE0_CLOSED`, `DEMA_ACTIVE_LOCAL` and `ONE_NODE_72H_HEALTH_PROVEN` all remain false. |
| Corridor Season consent bridge (NODE0-CORRIDOR-SEASON-CONSENT-BRIDGE-1A) | `packages/mission/src/corridor-season-consent-bridge.js` + `packages/mission/src/executing-repository-binding.js` + `apps/cli/src/commands/mission.js` + `tests/node0-corridor-season-consent-bridge.test.js` (25) + `tests/node0-season-action-authority-non-claims.test.js`. [MEASURED] [LOCAL_ONLY] The corridor consent preflight now requires an authoritative verified Season State and independently measured executing-repository binding before constructing root-bound consent. The preflight verifies consent only; it claims no independent FATE policy decision and executes no effect. Season State is loaded through `loadSeasonHead` under `--season <id> --dema-home <path>` (a caller-supplied state file is never authority, preserving the `head_candidates_conflict` ambiguity refusal); the executing commit and tree are measured from `git` via the repository's injected-runner idiom, so the state supplies the **claimed** values and the runtime supplies the **expected** ones. `evaluateCorridorWriteConsent` remains the canonical consent owner — `PERMIT_PREVIEW` here means EXACT CONTEXT-BOUND CONSENT VERIFIED, not FATE permitted, not execution authorized, not a nonce claimed, not an effect. The preflight returns before the nonce claim on every path. `authority_delta: 0` throughout. **`FATE_CONTRACT_GAP_CONFIRMED = true`** and **`FATE_EFFECT_ROUTE_INTEGRATION_ABSENT = true`**: `packages/fate/src/fate.js` is a 13-line exact-phrase consent helper, so no independent constitutional FATE policy contract exists yet; the bridge deliberately does not import it. Does NOT prove: `FATE_EFFECT_ROUTE_INTEGRATED`, `CORRIDOR_RENAME_SEASON_GATE_LIVE`, `MUST_NOT_REPEAT_EFFECT_GATE_ENFORCED`, `NODE0_LOCAL_MISSION_PROVEN`, `DEMA_ACTIVE_LOCAL`, `NODE0_CLOSED`, that any effect is reversible, or that a rename ever ran. |
