# Wrapper Knowledge
Wrappers are anti-corruption boundaries.

They normalize foreign semantics into exact identity, typed I/O, capabilities, side effects, effective config, evidence, and stable reason codes.

A wrapper reduces integration ambiguity. It does not certify truth or grant authority.

Prefer a thin wrapper over modifying third-party internals when the novelty delta is only integration semantics.
