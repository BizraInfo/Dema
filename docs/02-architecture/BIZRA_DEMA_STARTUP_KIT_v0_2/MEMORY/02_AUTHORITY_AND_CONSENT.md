# Authority & Consent Memory

truth_status: POLICY_BOUND
verified_at: 2026-08-25
verification_path: current FATE/consent state always overrides this summary

## Laws

- Tool visibility is not permission.
- Installed plugin is not authorized capability.
- Broad trust language does not silently authorize future push/merge/delete/external write.
- Failure never widens authority.
- Restart never widens authority.
- Ordinary data stewardship authority_delta must remain 0.

## Current Git transition

The local G6 candidate was reported `READY_FOR_COMMIT_GO`; the human explicitly supplied the word `commit` in the session. That consent applies only to the exact qualified local candidate, subject to re-verifying zero drift and exact tree identity. It does not authorize push.

If exact candidate identity cannot be re-established, the consent cannot be transferred to changed bytes.
