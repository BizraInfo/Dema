# BIZRA DEMA Startup Kit v0.2

**Status:** PROPOSED_DESIGN_SPECIFICATION + CURRENT_STATE_BINDING  
**Authority:** ZERO — this package grants no runtime, filesystem, network, Git, Google Drive, model, wallet, token, or federation authority.

## Purpose

v0.2 turns the v0.1 startup pack into an operational re-entry package for Node0. It adds:

- a current-state map that separates remote canon, local qualified candidate, measured runtime evidence, and open proof gaps;
- a step-by-step Node0 closure roadmap and local-agent runbook;
- durable memory slots rather than relying on chat context;
- a Google Knowledge System contract for Drive-derived sources;
- a Node0 data-estate organization model and processing pipeline;
- a Definition of Done for Node0 closure and data stewardship;
- the pluggable-capability / anchored-constitution law;
- observer/actuator separation for world adapters;
- explicit installation guidance for the `dema-data-steward` skill.

## Constitutional law

> **Anchor the laws. Pluginize capability.**

Models, tools, crawlers, browsers, memory engines, RAG systems, UI surfaces, storage engines, and OS actuators may be replaceable capability providers. Human sovereignty, Root identity, authority conservation, FATE semantics, consent law, receipt validity, UNKNOWN semantics, provenance law, and responsibility-carrying transition rules are not ordinary plugins.

## Core non-equivalences

`implemented != verified != canonical != authorized`  
`trace != diagnosis`  
`actor claim != observed effect`  
`memory != authority`  
`index != truth`  
`Google Drive source != Node0 authoritative memory`  
`plugin installed != plugin qualified != plugin authorized`  
`test green != production closed`

## Boot principle

Load only the minimum evidence-complete context. Current disk/runtime observations and current Git identity outrank cached summaries. Boot success grants no new authority.

## Important current boundary

This package must not be mixed into the already-qualified G6 canonicalization candidate before that candidate is committed on exact verified bytes. Treat this v0.2 package as the *next slice* after canonicalization.
