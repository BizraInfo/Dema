# Receipt: BIZRA-PROMPT-COMPILER-0A

Truth label: `BIZRA_PROMPT_COMPILER_MEASURED_REPO`

## Slice

Founder-language prompt compiler: dense invocations compile into typed,
phase-ordered mission contracts — vocabulary → ontology → operators →
execution graph → proof gates → one spearpoint.

## Proof Contract

- OPERATOR_TABLE binds each founder term to ONE role and ONE phase of the
  12-step chain; binding is the only path to "executed machinery".
- Precedence law pinned: comprehensive perception and minimum special case
  are RELATED BY PRECEDENCE (`perception > compression > amplification >
  one_spearpoint`), never peers.
- NO SILENT DROP is a reporting law: unbound doctrine names surface as
  UNCOMPILED_TOKENS warnings + a dedicated output section while recognized
  machinery still compiles.
- Structure-sensitive tamper probe: removing the spearpoint clause changes
  both content_hash and precedence_chain; whitespace changes neither.
- Deterministic: same text → same content_hash.

## What this does NOT prove

No mission execution, no model call, no claim that compiled missions pass
their own gates. Compilation is structure; performance lives behind FATE.

## Proof ran

Focused 5/5 · gate PASS (12 ops · 8 phases · chain verified) · registry row
BIZRA_PROMPT_COMPILER_0A (#89) · suite/check/guidance per closeout ladder.
