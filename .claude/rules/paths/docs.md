---
paths:
  - "docs/**"
  - "*.md"
---

# Path rule — documentation

Truth labels on every capability claim. Link evidence paths (file + test) for `MEASURED` rows.

`CURRENT_LIMITS.md` is the honesty map — keep it current.

Do not run Layer 1 artifact-safety on prose docs expecting PUBLIC_SAFE (see `CURRENT_LIMITS.md` verify section).

New boundaries: prefer `docs/06-adr/ADR-NNN-*.md` over growing `ARCHITECTURE.md` blindly.
