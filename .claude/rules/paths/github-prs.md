---
paths:
  - ".github/**"
---

# Path rule — GitHub and PRs

No force push to main. No CI workflow edits without explicit scoped GO.

PR = one proof story. Include test plan with exact commands run.

Do not post public GitHub comments without operator GO.

Dependabot / release workflows: `PLANNED` in `CURRENT_LIMITS.md` — treat as future slices.
